//SSZOS24  JOB 60798,E002884,
//         CLASS=W,
//         MSGCLASS=Q,
//         MSGLEVEL=(1,1),
//         NOTIFY=ROSCAAO,
//         TIME=NOLIMIT,
//         LINES=(999999,WARNING),
//         REGION=0M
//*
//*  MODIFY THE JOB STATEMENT ABOVE AS REQUIRED FOR YOUR INSTALLATION'S
//*  REQUIREMENTS, AND PRESS END TO CONTINUE
//*
//*
//JOBLIB   DD DSN=ZOS24.OS241147.ORDER.SCPPLOAD,
//            DISP=SHR
//*
//*--------------------------------------------------------------------
//* GDE: ServerPac INSTALLATION
//* DOC: STEP ALLOCDS WILL ALLOCATE AND CATALOG ALL DATASETS.
//*
//* NOTE:MSGID IS ADDED TO THE TSO PROFILE TO ENSURE THAT THE FIRST
//*      STEP CAN RUN SUCCESSFULLY.  PLEASE RESET YOUR PROFILE TO
//*      NOMSGID (IF REQUIRED) AFTER THE JOB HAS RUN.
//*
//* MRC: THE MAXIMUM EXPECTED RETURN CODE IS: 0
//*--------------------------------------------------------------------
//*************************************************************
//*   THE FOLLOWING STEP MAKES SURE THAT NONE OF THE DATA SETS
//*   TO BE ALLOCATED ALREADY HAVE EXISTING CATALOG ENTRIES
//*************************************************************
//*
//CHKDSN1  EXEC PGM=IKJEFT1B,COND=(4000,LT)
//SYSEXEC  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPCENU
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQEXEC
//         DD DISP=SHR,DSN=AOP.SAOPEXEC
//         DD DISP=SHR,DSN=EUVF.SEUVFEXC
//         DD DISP=SHR,DSN=SYS1.SEDGEXE1
//         DD DISP=SHR,DSN=EOY.SEOYCLIB
//         DD DISP=SHR,DSN=EOX.SEPHCLB1
//         DD DISP=SHR,DSN=ISP.SISPEXEC
//         DD DISP=SHR,DSN=IOE.SIOEEXEC
//         DD DISP=SHR,DSN=SYS1.SBPXEXEC
//         DD DISP=SHR,DSN=ISF.SISFEXEC
//         DD DISP=SHR,DSN=GLD.SGLDEXEC
//         DD DISP=SHR,DSN=FFST.V120ESA.SEPWSRC1
//SYSPROC  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPCENU
//         DD DISP=SHR,DSN=CPAC.CMDPROC
//         DD DISP=SHR,DSN=SYS1.SBLSCLI0
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJCLST
//         DD DISP=SHR,DSN=CBC.SCCNUTL
//         DD DISP=SHR,DSN=SYS1.DGTCLIB
//         DD DISP=SHR,DSN=CSF.SCSFCLI0
//         DD DISP=SHR,DSN=EOY.SEOYCLIB
//         DD DISP=SHR,DSN=EOX.SEPHCLB1
//         DD DISP=SHR,DSN=SYS1.SERBCLS
//         DD DISP=SHR,DSN=ISP.SISPCLIB
//         DD DISP=SHR,DSN=SYS1.HRFCLST
//         DD DISP=SHR,DSN=SYS1.SBDTCLI0
//         DD DISP=SHR,DSN=GIM.SGIMCLS0
//         DD DISP=SHR,DSN=ICQ.ICQCCLIB
//         DD DISP=SHR,DSN=SYS1.SCBDCLST
//         DD DISP=SHR,DSN=GDDM.SADMSAM
//         DD DISP=SHR,DSN=FFST.V120ESA.SEPWCENU
//ISPMLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPMENU
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJMLIB
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQMSGE
//         DD DISP=SHR,DSN=TCPIP.SEZAMENU
//         DD DISP=SHR,DSN=AOP.SAOPMENU
//         DD DISP=SHR,DSN=SYS1.DGTMLIB
//         DD DISP=SHR,DSN=SYS1.DFQMLIB
//         DD DISP=SHR,DSN=SYS1.SEDGMENU
//         DD DISP=SHR,DSN=CSF.SCSFMSG0
//         DD DISP=SHR,DSN=EOY.SEOYBENU
//         DD DISP=SHR,DSN=EOY.SEOYMENU
//         DD DISP=SHR,DSN=EOX.SEPHMSG1
//         DD DISP=SHR,DSN=SYS1.SERBMENU
//         DD DISP=SHR,DSN=ISP.SISPMENU
//         DD DISP=SHR,DSN=SYS1.HRFMSG
//         DD DISP=SHR,DSN=SYS1.SBDTMSG
//         DD DISP=SHR,DSN=SYS1.SBLSMSG0
//         DD DISP=SHR,DSN=SYS1.SBPXMENU
//         DD DISP=SHR,DSN=ISF.SISFMLIB
//         DD DISP=SHR,DSN=GIM.SGIMMENU
//         DD DISP=SHR,DSN=ICQ.ICQMLIB
//         DD DISP=SHR,DSN=SYS1.SCBDMENU
//         DD DISP=SHR,DSN=GDDM.SADMMSG
//ISPPLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPPENU
//         DD DISP=SHR,DSN=CPAC.ISPPLIB
//         DD DISP=SHR,DSN=ISP.SISPPENU
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJPLIB
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQPNLE
//         DD DISP=SHR,DSN=TCPIP.SEZAPENU
//         DD DISP=SHR,DSN=SYS1.SBLSPNL0
//         DD DISP=SHR,DSN=AOP.SAOPPENU
//         DD DISP=SHR,DSN=SYS1.DFQPLIB
//         DD DISP=SHR,DSN=SYS1.DGTPLIB
//         DD DISP=SHR,DSN=SYS1.SEDGPENU
//         DD DISP=SHR,DSN=CSF.SCSFPNL0
//         DD DISP=SHR,DSN=EOY.SEOYPENU
//         DD DISP=SHR,DSN=EOX.SEPHPNL1
//         DD DISP=SHR,DSN=SYS1.SERBPENU
//         DD DISP=SHR,DSN=SYS1.HRFPANL
//         DD DISP=SHR,DSN=SYS1.SBDTPNL0
//         DD DISP=SHR,DSN=SYS1.SHASPNL0
//         DD DISP=SHR,DSN=SYS1.SBPXPENU
//         DD DISP=SHR,DSN=ISF.SISFPLIB
//         DD DISP=SHR,DSN=GIM.SGIMPENU
//         DD DISP=SHR,DSN=ICQ.ICQPLIB
//         DD DISP=SHR,DSN=SYS1.SCBDPENU
//         DD DISP=SHR,DSN=GDDM.SADMPNL
//         DD DISP=SHR,DSN=FFST.V120ESA.SEPWPENU
//ISPSLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPSENU
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJSLIB
//         DD DISP=SHR,DSN=SYS1.DGTSLIB
//         DD DISP=SHR,DSN=CSF.SCSFSKL0
//         DD DISP=SHR,DSN=ISP.SISPSLIB
//         DD DISP=SHR,DSN=ISP.SISPSENU
//         DD DISP=SHR,DSN=SYS1.HRFSKEL
//         DD DISP=SHR,DSN=SYS1.SBLSKEL0
//         DD DISP=SHR,DSN=ISF.SISFSLIB
//         DD DISP=SHR,DSN=GIM.SGIMSENU
//         DD DISP=SHR,DSN=ICQ.ICQSLIB
//ISPLLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPLOAD
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJMOD1
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQANLE
//         DD DISP=SHR,DSN=SYS1.DFQLLIB
//         DD DISP=SHR,DSN=SYS1.DGTLLIB
//ISPTLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPTENU
//         DD DISP=SHR,DSN=E002884.ISPF.ISPPROF
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJTLIB
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQTBLE
//         DD DISP=SHR,DSN=SYS1.DGTTLIB
//         DD DISP=SHR,DSN=CSF.SCSFTLIB
//         DD DISP=SHR,DSN=EOY.SEOYTENU
//         DD DISP=SHR,DSN=EOX.SEPHTBL1
//         DD DISP=SHR,DSN=SYS1.SERBT
//         DD DISP=SHR,DSN=SYS1.SERBTENU
//         DD DISP=SHR,DSN=ISP.SISPTENU
//         DD DISP=SHR,DSN=SYS1.SBLSTBL0
//         DD DISP=SHR,DSN=SYS1.SBPXTENU
//         DD DISP=SHR,DSN=ISF.SISFTLIB
//         DD DISP=SHR,DSN=GIM.SGIMTENU
//         DD DISP=SHR,DSN=ICQ.ICQTABLS
//         DD DISP=SHR,DSN=SYS1.SCBDTENU
//ISPLOG   DD DSN=&ISPLOG,
//            DISP=NEW,UNIT=SYSALLDA,SPACE=(CYL,(1,1,1)),
//            DCB=(RECFM=FB,BLKSIZE=3120,LRECL=80)
//ISPPROF  DD DSN=&ISPPROF,
//            DISP=NEW,UNIT=SYSALLDA,SPACE=(CYL,(1,1,1)),
//            DCB=(RECFM=FB,BLKSIZE=3120,LRECL=80)
//SYSPRINT DD SYSOUT=*
//SYSTSPRT DD SYSOUT=*
//SYSTSIN  DD *
 PROFILE PREFIX(E002884) MSGID
 ISPSTART CMD(%CPPEDSN NOTEXIST F )
/*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKDSN1),(4000,LT))
//*
//*
//*************************************************************
//*
//*  THIS IS TO CHECK WHETHER SMS IS ACTIVE IN THE DRIVING
//*  SYSTEM. IT USES THE REXX MVSVAR FUNCTION TO CHECK IF SMS
//*  IS ACTIVE AND RETURN THE CORRESPONDING RETURN CODE.
//*
//*  IF SMS
//*       ACTIVE   ---    RETURN 0
//*     INACTIVE   ---    RETURN 4
//*  UNAVAILABLE   ---    RETURN 8
//*    ALL OTHER   ---    RETURN 12
//*
//*************************************************************
//CHKSMS1  EXEC PGM=IKJEFT1B
//SYSEXEC  DD DSN=ZOS24.OS241147.ORDER.SCPPCENU,DISP=SHR
//SYSTSPRT DD SYSOUT=*
//SYSTSIN  DD *
  %CPPESMS NOISPF
/*
//*
//NOTOK EXEC  PGM=CPPMAXRC,COND=((0,GE,CHKSMS1),(4000,LT))
//*
//*************************************************************
//*
//*************************************************************
//*
//ALCONTIG EXEC PGM=IEFBR14,COND=(4000,LT)
//BRODCAST DD DSN=Z24MST.SYS1.BRODCAST,
//      DISP=(NEW,CATLG,DELETE),
//      UNIT=3390,
//      VOL=SER=ZOS247,
//      DCB=(BLKSIZE=129,RECFM=F,
//      LRECL=129,
//      DSORG=PS),
//      SPACE=(CYL,(3,0),,CONTIG)
//HASPACE DD DSN=Z24MST.SYS1.HASPACE,
//      DISP=(NEW,CATLG,DELETE),
//      UNIT=3390,
//      VOL=SER=ZOS247,
//      DCB=(BLKSIZE=3992,RECFM=U,
//      LRECL=3992,
//      DSORG=PSU),
//      SPACE=(CYL,(234,0),,CONTIG)
//HASPCKPT DD DSN=Z24MST.SYS1.HASPCKPT,
//      DISP=(NEW,CATLG,DELETE),
//      UNIT=3390,
//      VOL=SER=ZOS247,
//      DCB=(BLKSIZE=4096,RECFM=F,
//      LRECL=4096,
//      DSORG=PS),
//      SPACE=(CYL,(2,0),,CONTIG)
//HASPCKP2 DD DSN=Z24MST.SYS1.HASPCKP2,
//      DISP=(NEW,CATLG,DELETE),
//      UNIT=3390,
//      VOL=SER=ZOS247,
//      DCB=(BLKSIZE=4096,RECFM=F,
//      LRECL=4096,
//      DSORG=PS),
//      SPACE=(CYL,(2,0),,CONTIG)
//NUCLEUS DD DSN=Z24MST.SYS1.NUCLEUS,
//      DISP=(NEW,CATLG,DELETE),
//      UNIT=3390,
//      VOL=SER=ZOS240,
//      DCB=(BLKSIZE=32760,RECFM=U,
//      LRECL=0,
//      DSORG=PO),
//      SPACE=(TRK,(1264,0,124),,CONTIG),
//      DSNTYPE=PDS
//RACFPRIM DD DSN=Z24MST.SYS1.RACF,
//      DISP=(NEW,CATLG,DELETE),
//      UNIT=3390,
//      VOL=SER=ZOS247,
//      DCB=(BLKSIZE=4096,RECFM=F,
//      LRECL=4096,
//      DSORG=PS),
//      SPACE=(CYL,(25,0),,CONTIG)
//RACFBACK DD DSN=Z24MST.SYS1.RACFSEC,
//      DISP=(NEW,CATLG,DELETE),
//      UNIT=3390,
//      VOL=SER=ZOS247,
//      DCB=(BLKSIZE=4096,RECFM=F,
//      LRECL=4096,
//      DSORG=PS),
//      SPACE=(CYL,(25,0),,CONTIG)
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ALCONTIG),(4000,LT))
//*
//*************************************************************
//*
//*  THIS IS TO CHECK WHETHER SMS IS ACTIVE IN THE DRIVING
//*  SYSTEM. IT USES THE REXX MVSVAR FUNCTION TO CHECK IF SMS
//*  IS ACTIVE AND RETURN THE CORRESPONDING RETURN CODE.
//*
//*  IF SMS
//*       ACTIVE   ---    RETURN 0
//*     INACTIVE   ---    RETURN 4
//*  UNAVAILABLE   ---    RETURN 8
//*    ALL OTHER   ---    RETURN 12
//*
//*************************************************************
//CHKSMS2  EXEC PGM=IKJEFT1B
//SYSEXEC  DD DSN=ZOS24.OS241147.ORDER.SCPPCENU,DISP=SHR
//SYSTSPRT DD SYSOUT=*
//SYSTSIN  DD *
  %CPPESMS NOISPF
/*
//*
//NOTOK EXEC  PGM=CPPMAXRC,COND=((0,GE,CHKSMS2),(4000,LT))
//*
//*************************************************************
//* ALLOCATE THE ZFS FILE SYSTEMS
//*************************************************************
//*
//ALCZFS EXEC PGM=IKJEFT01,DYNAMNBR=500,COND=(4000,LT)
//SYSTSPRT DD SYSOUT=*
//SYSTSIN    DD *
    ALLOC -
        DSNAME('Z24SUP.FNT.OMVS.ZFS') -
        RECORG(LS) -
        TRACKS SPACE(307,31) -
        BLKSIZE(4096) -
        VOLUME(ZOS243) -
        UNIT(3390) -
        CATALOG
    ALLOC -
        DSNAME('Z24SUP.OMVS.ETC') -
        RECORG(LS) -
        TRACKS SPACE(47,5) -
        BLKSIZE(4096) -
        VOLUME(ZOS247) -
        UNIT(3390) -
        CATALOG
    ALLOC -
        DSNAME('Z24SUP.OMVS.JAVA64V8') -
        RECORG(LS) -
        TRACKS SPACE(15378,1538) -
        BLKSIZE(4096) -
        VOLUME(ZOS243) -
        UNIT(3390) -
        CATALOG
    ALLOC -
        DSNAME('Z24SUP.OMVS.JV31V80') -
        RECORG(LS) -
        TRACKS SPACE(12631,1264) -
        BLKSIZE(4096) -
        VOLUME(ZOS242) -
        UNIT(3390) -
        CATALOG
    ALLOC -
        DSNAME('Z24SUP.OMVS.NETVHFS') -
        RECORG(LS) -
        TRACKS SPACE(1092,110) -
        BLKSIZE(4096) -
        VOLUME(ZOS243) -
        UNIT(3390) -
        CATALOG
    ALLOC -
        DSNAME('Z24SUP.OMVS.SAZDZFS') -
        RECORG(LS) -
        TRACKS SPACE(19095,1910) -
        BLKSIZE(4096) -
        VOLUME(ZOS242) -
        UNIT(3390) -
        CATALOG
    ALLOC -
        DSNAME('Z24SUP.OMVS.SBAQROOT') -
        RECORG(LS) -
        TRACKS SPACE(14711,1472) -
        BLKSIZE(4096) -
        VOLUME(ZOS247) -
        UNIT(3390) -
        CATALOG
    ALLOC -
        DSNAME('Z24SUP.OMVS.SBBLZFS') -
        RECORG(LS) -
        TRACKS SPACE(28010,2801) -
        BLKSIZE(4096) -
        VOLUME(ZOS242) -
        UNIT(3390) -
        CATALOG
    ALLOC -
        DSNAME('Z24SUP.OMVS.SCFZHFS2') -
        RECORG(LS) -
        TRACKS SPACE(1239,124) -
        BLKSIZE(4096) -
        VOLUME(ZOS247) -
        UNIT(3390) -
        CATALOG
    ALLOC -
        DSNAME('Z24SUP.OMVS.SIBMROOT') -
        RECORG(LS) -
        TRACKS SPACE(28,3) -
        BLKSIZE(4096) -
        VOLUME(ZOS241) -
        UNIT(3390) -
        CATALOG
    ALLOC -
        DSNAME('Z24SUP.OMVS.SIGYROOT') -
        RECORG(LS) -
        TRACKS SPACE(12,2) -
        BLKSIZE(4096) -
        VOLUME(ZOS242) -
        UNIT(3390) -
        CATALOG
    ALLOC -
        DSNAME('Z24SUP.OMVS.SIXMHFS') -
        RECORG(LS) -
        TRACKS SPACE(16095,1610) -
        BLKSIZE(4096) -
        VOLUME(ZOS243) -
        UNIT(3390) -
        CATALOG
    ALLOC -
        DSNAME('Z24SUP.OMVS.SIZUUSRD') -
        RECORG(LS) -
        TRACKS SPACE(87,9) -
        BLKSIZE(4096) -
        VOLUME(ZOS247) -
        UNIT(3390) -
        CATALOG
    ALLOC -
        DSNAME('Z24SUP.OMVS.TKANROOT') -
        RECORG(LS) -
        TRACKS SPACE(430,43) -
        BLKSIZE(4096) -
        VOLUME(ZOS242) -
        UNIT(3390) -
        CATALOG
    ALLOC -
        DSNAME('Z24SUP.OMVS.VAR') -
        RECORG(LS) -
        TRACKS SPACE(298,30) -
        BLKSIZE(4096) -
        VOLUME(ZOS247) -
        UNIT(3390) -
        CATALOG
    ALLOC -
        DSNAME('Z24SUP.OMVS.V9R1MX.MQROOT') -
        RECORG(LS) -
        TRACKS SPACE(18094,1810) -
        BLKSIZE(4096) -
        VOLUME(ZOS243) -
        UNIT(3390) -
        CATALOG
    ALLOC -
        DSNAME('Z24SUP.OMVS.ROOT') -
        RECORG(LS) -
        TRACKS SPACE(68237,6824) -
        BLKSIZE(4096) -
        VOLUME(ZOS243) -
        UNIT(3390) -
        CATALOG
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ALCZFS),(4000,LT))
//*
//*************************************************************
//* Check that VSAM LDS data sets are allocated successfully.
//* Format the VSAM LDS as a compatibility mode aggregate and
//* create a zFS file system
//* -Parameters -compat and -aggregate should be lower case
//*******************************************************************/
//*
//CHKZFS1  EXEC PGM=IDCAMS,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *
  LISTCAT -
     ENTRY(Z24SUP.FNT.OMVS.ZFS)
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKZFS1),(4000,LT))
//*
//ZFSMNT1  EXEC PGM=IOEAGFMT,REGION=0M,COND=(4000,LT),
//  PARM=('-aggregate Z24SUP.FNT.OMVS.ZFS
//             -compat')
//SYSPRINT DD SYSOUT=*
//STDOUT   DD SYSOUT=*
//STDERR   DD SYSOUT=*
//SYSUDUMP DD SYSOUT=*
//CEEDUMP  DD SYSOUT=*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ZFSMNT1),(4000,LT))
//*
//CHKZFS2  EXEC PGM=IDCAMS,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *
  LISTCAT -
     ENTRY(Z24SUP.OMVS.ETC)
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKZFS2),(4000,LT))
//*
//ZFSMNT2  EXEC PGM=IOEAGFMT,REGION=0M,COND=(4000,LT),
//  PARM=('-aggregate Z24SUP.OMVS.ETC
//             -compat')
//SYSPRINT DD SYSOUT=*
//STDOUT   DD SYSOUT=*
//STDERR   DD SYSOUT=*
//SYSUDUMP DD SYSOUT=*
//CEEDUMP  DD SYSOUT=*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ZFSMNT2),(4000,LT))
//*
//CHKZFS3  EXEC PGM=IDCAMS,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *
  LISTCAT -
     ENTRY(Z24SUP.OMVS.JAVA64V8)
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKZFS3),(4000,LT))
//*
//ZFSMNT3  EXEC PGM=IOEAGFMT,REGION=0M,COND=(4000,LT),
//  PARM=('-aggregate Z24SUP.OMVS.JAVA64V8
//             -compat')
//SYSPRINT DD SYSOUT=*
//STDOUT   DD SYSOUT=*
//STDERR   DD SYSOUT=*
//SYSUDUMP DD SYSOUT=*
//CEEDUMP  DD SYSOUT=*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ZFSMNT3),(4000,LT))
//*
//CHKZFS4  EXEC PGM=IDCAMS,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *
  LISTCAT -
     ENTRY(Z24SUP.OMVS.JV31V80)
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKZFS4),(4000,LT))
//*
//ZFSMNT4  EXEC PGM=IOEAGFMT,REGION=0M,COND=(4000,LT),
//  PARM=('-aggregate Z24SUP.OMVS.JV31V80
//             -compat')
//SYSPRINT DD SYSOUT=*
//STDOUT   DD SYSOUT=*
//STDERR   DD SYSOUT=*
//SYSUDUMP DD SYSOUT=*
//CEEDUMP  DD SYSOUT=*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ZFSMNT4),(4000,LT))
//*
//CHKZFS5  EXEC PGM=IDCAMS,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *
  LISTCAT -
     ENTRY(Z24SUP.OMVS.NETVHFS)
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKZFS5),(4000,LT))
//*
//ZFSMNT5  EXEC PGM=IOEAGFMT,REGION=0M,COND=(4000,LT),
//  PARM=('-aggregate Z24SUP.OMVS.NETVHFS
//             -compat')
//SYSPRINT DD SYSOUT=*
//STDOUT   DD SYSOUT=*
//STDERR   DD SYSOUT=*
//SYSUDUMP DD SYSOUT=*
//CEEDUMP  DD SYSOUT=*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ZFSMNT5),(4000,LT))
//*
//CHKZFS6  EXEC PGM=IDCAMS,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *
  LISTCAT -
     ENTRY(Z24SUP.OMVS.SAZDZFS)
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKZFS6),(4000,LT))
//*
//ZFSMNT6  EXEC PGM=IOEAGFMT,REGION=0M,COND=(4000,LT),
//  PARM=('-aggregate Z24SUP.OMVS.SAZDZFS
//             -compat')
//SYSPRINT DD SYSOUT=*
//STDOUT   DD SYSOUT=*
//STDERR   DD SYSOUT=*
//SYSUDUMP DD SYSOUT=*
//CEEDUMP  DD SYSOUT=*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ZFSMNT6),(4000,LT))
//*
//CHKZFS7  EXEC PGM=IDCAMS,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *
  LISTCAT -
     ENTRY(Z24SUP.OMVS.SBAQROOT)
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKZFS7),(4000,LT))
//*
//ZFSMNT7  EXEC PGM=IOEAGFMT,REGION=0M,COND=(4000,LT),
//  PARM=('-aggregate Z24SUP.OMVS.SBAQROOT
//             -compat')
//SYSPRINT DD SYSOUT=*
//STDOUT   DD SYSOUT=*
//STDERR   DD SYSOUT=*
//SYSUDUMP DD SYSOUT=*
//CEEDUMP  DD SYSOUT=*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ZFSMNT7),(4000,LT))
//*
//CHKZFS8  EXEC PGM=IDCAMS,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *
  LISTCAT -
     ENTRY(Z24SUP.OMVS.SBBLZFS)
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKZFS8),(4000,LT))
//*
//ZFSMNT8  EXEC PGM=IOEAGFMT,REGION=0M,COND=(4000,LT),
//  PARM=('-aggregate Z24SUP.OMVS.SBBLZFS
//             -compat')
//SYSPRINT DD SYSOUT=*
//STDOUT   DD SYSOUT=*
//STDERR   DD SYSOUT=*
//SYSUDUMP DD SYSOUT=*
//CEEDUMP  DD SYSOUT=*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ZFSMNT8),(4000,LT))
//*
//CHKZFS9  EXEC PGM=IDCAMS,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *
  LISTCAT -
     ENTRY(Z24SUP.OMVS.SCFZHFS2)
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKZFS9),(4000,LT))
//*
//ZFSMNT9  EXEC PGM=IOEAGFMT,REGION=0M,COND=(4000,LT),
//  PARM=('-aggregate Z24SUP.OMVS.SCFZHFS2
//             -compat')
//SYSPRINT DD SYSOUT=*
//STDOUT   DD SYSOUT=*
//STDERR   DD SYSOUT=*
//SYSUDUMP DD SYSOUT=*
//CEEDUMP  DD SYSOUT=*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ZFSMNT9),(4000,LT))
//*
//CHKZFS10  EXEC PGM=IDCAMS,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *
  LISTCAT -
     ENTRY(Z24SUP.OMVS.SIBMROOT)
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKZFS10),(4000,LT))
//*
//ZFSMNT10  EXEC PGM=IOEAGFMT,REGION=0M,COND=(4000,LT),
//  PARM=('-aggregate Z24SUP.OMVS.SIBMROOT
//             -compat')
//SYSPRINT DD SYSOUT=*
//STDOUT   DD SYSOUT=*
//STDERR   DD SYSOUT=*
//SYSUDUMP DD SYSOUT=*
//CEEDUMP  DD SYSOUT=*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ZFSMNT10),(4000,LT))
//*
//CHKZFS11  EXEC PGM=IDCAMS,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *
  LISTCAT -
     ENTRY(Z24SUP.OMVS.SIGYROOT)
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKZFS11),(4000,LT))
//*
//ZFSMNT11  EXEC PGM=IOEAGFMT,REGION=0M,COND=(4000,LT),
//  PARM=('-aggregate Z24SUP.OMVS.SIGYROOT
//             -compat')
//SYSPRINT DD SYSOUT=*
//STDOUT   DD SYSOUT=*
//STDERR   DD SYSOUT=*
//SYSUDUMP DD SYSOUT=*
//CEEDUMP  DD SYSOUT=*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ZFSMNT11),(4000,LT))
//*
//CHKZFS12  EXEC PGM=IDCAMS,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *
  LISTCAT -
     ENTRY(Z24SUP.OMVS.SIXMHFS)
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKZFS12),(4000,LT))
//*
//ZFSMNT12  EXEC PGM=IOEAGFMT,REGION=0M,COND=(4000,LT),
//  PARM=('-aggregate Z24SUP.OMVS.SIXMHFS
//             -compat')
//SYSPRINT DD SYSOUT=*
//STDOUT   DD SYSOUT=*
//STDERR   DD SYSOUT=*
//SYSUDUMP DD SYSOUT=*
//CEEDUMP  DD SYSOUT=*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ZFSMNT12),(4000,LT))
//*
//CHKZFS13  EXEC PGM=IDCAMS,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *
  LISTCAT -
     ENTRY(Z24SUP.OMVS.SIZUUSRD)
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKZFS13),(4000,LT))
//*
//ZFSMNT13  EXEC PGM=IOEAGFMT,REGION=0M,COND=(4000,LT),
//  PARM=('-aggregate Z24SUP.OMVS.SIZUUSRD
//             -compat')
//SYSPRINT DD SYSOUT=*
//STDOUT   DD SYSOUT=*
//STDERR   DD SYSOUT=*
//SYSUDUMP DD SYSOUT=*
//CEEDUMP  DD SYSOUT=*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ZFSMNT13),(4000,LT))
//*
//CHKZFS14  EXEC PGM=IDCAMS,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *
  LISTCAT -
     ENTRY(Z24SUP.OMVS.TKANROOT)
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKZFS14),(4000,LT))
//*
//ZFSMNT14  EXEC PGM=IOEAGFMT,REGION=0M,COND=(4000,LT),
//  PARM=('-aggregate Z24SUP.OMVS.TKANROOT
//             -compat')
//SYSPRINT DD SYSOUT=*
//STDOUT   DD SYSOUT=*
//STDERR   DD SYSOUT=*
//SYSUDUMP DD SYSOUT=*
//CEEDUMP  DD SYSOUT=*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ZFSMNT14),(4000,LT))
//*
//CHKZFS15  EXEC PGM=IDCAMS,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *
  LISTCAT -
     ENTRY(Z24SUP.OMVS.VAR)
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKZFS15),(4000,LT))
//*
//ZFSMNT15  EXEC PGM=IOEAGFMT,REGION=0M,COND=(4000,LT),
//  PARM=('-aggregate Z24SUP.OMVS.VAR
//             -compat')
//SYSPRINT DD SYSOUT=*
//STDOUT   DD SYSOUT=*
//STDERR   DD SYSOUT=*
//SYSUDUMP DD SYSOUT=*
//CEEDUMP  DD SYSOUT=*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ZFSMNT15),(4000,LT))
//*
//CHKZFS16  EXEC PGM=IDCAMS,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *
  LISTCAT -
     ENTRY(Z24SUP.OMVS.V9R1MX.MQROOT)
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKZFS16),(4000,LT))
//*
//ZFSMNT16  EXEC PGM=IOEAGFMT,REGION=0M,COND=(4000,LT),
//  PARM=('-aggregate Z24SUP.OMVS.V9R1MX.MQROOT
//             -compat')
//SYSPRINT DD SYSOUT=*
//STDOUT   DD SYSOUT=*
//STDERR   DD SYSOUT=*
//SYSUDUMP DD SYSOUT=*
//CEEDUMP  DD SYSOUT=*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ZFSMNT16),(4000,LT))
//*
//CHKZFS17  EXEC PGM=IDCAMS,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *
  LISTCAT -
     ENTRY(Z24SUP.OMVS.ROOT)
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKZFS17),(4000,LT))
//*
//ZFSMNT17  EXEC PGM=IOEAGFMT,REGION=0M,COND=(4000,LT),
//  PARM=('-aggregate Z24SUP.OMVS.ROOT
//             -compat')
//SYSPRINT DD SYSOUT=*
//STDOUT   DD SYSOUT=*
//STDERR   DD SYSOUT=*
//SYSUDUMP DD SYSOUT=*
//CEEDUMP  DD SYSOUT=*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ZFSMNT17),(4000,LT))
//*
//*************************************************************
//*   ENSURE THAT THE DATA SETS ALLOCATED WITH IEFBR14
//*   WERE ALLOCATED SUCCESSFULLY
//*************************************************************
//*
//CHKALLOC EXEC PGM=IDCAMS,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *
  LISTCAT -
     ENTRY(Z24MST.SYS1.BRODCAST)

  LISTCAT -
     ENTRY(Z24MST.SYS1.HASPACE)

  LISTCAT -
     ENTRY(Z24MST.SYS1.HASPCKPT)

  LISTCAT -
     ENTRY(Z24MST.SYS1.HASPCKP2)

  LISTCAT -
     ENTRY(Z24MST.SYS1.NUCLEUS)

  LISTCAT -
     ENTRY(Z24MST.SYS1.RACF)

  LISTCAT -
     ENTRY(Z24MST.SYS1.RACFSEC)

//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKALLOC),(4000,LT))
//*************************************************************
//*   THE FOLLOWING STEP MAKES SURE THAT THE *ONLY* POSSIBLE REASON
//*   FOR A NONZERO RETURN CODE FROM LISTCAT IS "DATA SET NOT FOUND".
//*************************************************************
//*
//CHKDSN2  EXEC PGM=IKJEFT1B,COND=(4000,LT)
//SYSEXEC  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPCENU
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQEXEC
//         DD DISP=SHR,DSN=AOP.SAOPEXEC
//         DD DISP=SHR,DSN=EUVF.SEUVFEXC
//         DD DISP=SHR,DSN=SYS1.SEDGEXE1
//         DD DISP=SHR,DSN=EOY.SEOYCLIB
//         DD DISP=SHR,DSN=EOX.SEPHCLB1
//         DD DISP=SHR,DSN=ISP.SISPEXEC
//         DD DISP=SHR,DSN=IOE.SIOEEXEC
//         DD DISP=SHR,DSN=SYS1.SBPXEXEC
//         DD DISP=SHR,DSN=ISF.SISFEXEC
//         DD DISP=SHR,DSN=GLD.SGLDEXEC
//         DD DISP=SHR,DSN=FFST.V120ESA.SEPWSRC1
//SYSPROC  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPCENU
//         DD DISP=SHR,DSN=CPAC.CMDPROC
//         DD DISP=SHR,DSN=SYS1.SBLSCLI0
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJCLST
//         DD DISP=SHR,DSN=CBC.SCCNUTL
//         DD DISP=SHR,DSN=SYS1.DGTCLIB
//         DD DISP=SHR,DSN=CSF.SCSFCLI0
//         DD DISP=SHR,DSN=EOY.SEOYCLIB
//         DD DISP=SHR,DSN=EOX.SEPHCLB1
//         DD DISP=SHR,DSN=SYS1.SERBCLS
//         DD DISP=SHR,DSN=ISP.SISPCLIB
//         DD DISP=SHR,DSN=SYS1.HRFCLST
//         DD DISP=SHR,DSN=SYS1.SBDTCLI0
//         DD DISP=SHR,DSN=GIM.SGIMCLS0
//         DD DISP=SHR,DSN=ICQ.ICQCCLIB
//         DD DISP=SHR,DSN=SYS1.SCBDCLST
//         DD DISP=SHR,DSN=GDDM.SADMSAM
//         DD DISP=SHR,DSN=FFST.V120ESA.SEPWCENU
//ISPMLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPMENU
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJMLIB
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQMSGE
//         DD DISP=SHR,DSN=TCPIP.SEZAMENU
//         DD DISP=SHR,DSN=AOP.SAOPMENU
//         DD DISP=SHR,DSN=SYS1.DGTMLIB
//         DD DISP=SHR,DSN=SYS1.DFQMLIB
//         DD DISP=SHR,DSN=SYS1.SEDGMENU
//         DD DISP=SHR,DSN=CSF.SCSFMSG0
//         DD DISP=SHR,DSN=EOY.SEOYBENU
//         DD DISP=SHR,DSN=EOY.SEOYMENU
//         DD DISP=SHR,DSN=EOX.SEPHMSG1
//         DD DISP=SHR,DSN=SYS1.SERBMENU
//         DD DISP=SHR,DSN=ISP.SISPMENU
//         DD DISP=SHR,DSN=SYS1.HRFMSG
//         DD DISP=SHR,DSN=SYS1.SBDTMSG
//         DD DISP=SHR,DSN=SYS1.SBLSMSG0
//         DD DISP=SHR,DSN=SYS1.SBPXMENU
//         DD DISP=SHR,DSN=ISF.SISFMLIB
//         DD DISP=SHR,DSN=GIM.SGIMMENU
//         DD DISP=SHR,DSN=ICQ.ICQMLIB
//         DD DISP=SHR,DSN=SYS1.SCBDMENU
//         DD DISP=SHR,DSN=GDDM.SADMMSG
//ISPPLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPPENU
//         DD DISP=SHR,DSN=CPAC.ISPPLIB
//         DD DISP=SHR,DSN=ISP.SISPPENU
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJPLIB
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQPNLE
//         DD DISP=SHR,DSN=TCPIP.SEZAPENU
//         DD DISP=SHR,DSN=SYS1.SBLSPNL0
//         DD DISP=SHR,DSN=AOP.SAOPPENU
//         DD DISP=SHR,DSN=SYS1.DFQPLIB
//         DD DISP=SHR,DSN=SYS1.DGTPLIB
//         DD DISP=SHR,DSN=SYS1.SEDGPENU
//         DD DISP=SHR,DSN=CSF.SCSFPNL0
//         DD DISP=SHR,DSN=EOY.SEOYPENU
//         DD DISP=SHR,DSN=EOX.SEPHPNL1
//         DD DISP=SHR,DSN=SYS1.SERBPENU
//         DD DISP=SHR,DSN=SYS1.HRFPANL
//         DD DISP=SHR,DSN=SYS1.SBDTPNL0
//         DD DISP=SHR,DSN=SYS1.SHASPNL0
//         DD DISP=SHR,DSN=SYS1.SBPXPENU
//         DD DISP=SHR,DSN=ISF.SISFPLIB
//         DD DISP=SHR,DSN=GIM.SGIMPENU
//         DD DISP=SHR,DSN=ICQ.ICQPLIB
//         DD DISP=SHR,DSN=SYS1.SCBDPENU
//         DD DISP=SHR,DSN=GDDM.SADMPNL
//         DD DISP=SHR,DSN=FFST.V120ESA.SEPWPENU
//ISPSLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPSENU
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJSLIB
//         DD DISP=SHR,DSN=SYS1.DGTSLIB
//         DD DISP=SHR,DSN=CSF.SCSFSKL0
//         DD DISP=SHR,DSN=ISP.SISPSLIB
//         DD DISP=SHR,DSN=ISP.SISPSENU
//         DD DISP=SHR,DSN=SYS1.HRFSKEL
//         DD DISP=SHR,DSN=SYS1.SBLSKEL0
//         DD DISP=SHR,DSN=ISF.SISFSLIB
//         DD DISP=SHR,DSN=GIM.SGIMSENU
//         DD DISP=SHR,DSN=ICQ.ICQSLIB
//ISPLLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPLOAD
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJMOD1
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQANLE
//         DD DISP=SHR,DSN=SYS1.DFQLLIB
//         DD DISP=SHR,DSN=SYS1.DGTLLIB
//ISPTLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPTENU
//         DD DISP=SHR,DSN=E002884.ISPF.ISPPROF
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJTLIB
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQTBLE
//         DD DISP=SHR,DSN=SYS1.DGTTLIB
//         DD DISP=SHR,DSN=CSF.SCSFTLIB
//         DD DISP=SHR,DSN=EOY.SEOYTENU
//         DD DISP=SHR,DSN=EOX.SEPHTBL1
//         DD DISP=SHR,DSN=SYS1.SERBT
//         DD DISP=SHR,DSN=SYS1.SERBTENU
//         DD DISP=SHR,DSN=ISP.SISPTENU
//         DD DISP=SHR,DSN=SYS1.SBLSTBL0
//         DD DISP=SHR,DSN=SYS1.SBPXTENU
//         DD DISP=SHR,DSN=ISF.SISFTLIB
//         DD DISP=SHR,DSN=GIM.SGIMTENU
//         DD DISP=SHR,DSN=ICQ.ICQTABLS
//         DD DISP=SHR,DSN=SYS1.SCBDTENU
//ISPLOG   DD DSN=&ISPLOG,
//            DISP=NEW,UNIT=SYSALLDA,SPACE=(CYL,(1,1,1)),
//            DCB=(RECFM=FB,BLKSIZE=3120,LRECL=80)
//ISPPROF  DD DSN=&ISPPROF,
//            DISP=NEW,UNIT=SYSALLDA,SPACE=(CYL,(1,1,1)),
//            DCB=(RECFM=FB,BLKSIZE=3120,LRECL=80)
//SYSPRINT DD SYSOUT=*
//SYSTSPRT DD SYSOUT=*
//SYSTSIN  DD *
 PROFILE PREFIX(E002884)
 ISPSTART CMD(%CPPEDSN CHECKRC F )
/*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKDSN2),(4000,LT))
//*
//*
//*************************************************************
//*
//*  THIS IS TO CHECK WHETHER SMS IS ACTIVE IN THE DRIVING
//*  SYSTEM. IT USES THE REXX MVSVAR FUNCTION TO CHECK IF SMS
//*  IS ACTIVE AND RETURN THE CORRESPONDING RETURN CODE.
//*
//*  IF SMS
//*       ACTIVE   ---    RETURN 0
//*     INACTIVE   ---    RETURN 4
//*  UNAVAILABLE   ---    RETURN 8
//*    ALL OTHER   ---    RETURN 12
//*
//*************************************************************
//CHKSMS4  EXEC PGM=IKJEFT1B
//SYSEXEC  DD DSN=ZOS24.OS241147.ORDER.SCPPCENU,DISP=SHR
//SYSTSPRT DD SYSOUT=*
//SYSTSIN  DD *
  %CPPESMS NOISPF
/*
//*
//NOTOK EXEC  PGM=CPPMAXRC,COND=((0,GE,CHKSMS4),(4000,LT))
//*
//*--------------------------------------------------------------------
//* DOC: STEP ALCVSAM ALLOCATES THE NEW SYSTEM'S VSAM DATASETS
//*--------------------------------------------------------------------
//*
//ALCVSAM  EXEC PGM=IDCAMS,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//PAGECOMM DD UNIT=3390,VOL=SER=ZOS247,DISP=OLD
//PAGELOC1 DD UNIT=3390,VOL=SER=ZOS247,DISP=OLD
//PAGEPLPA DD UNIT=3390,VOL=SER=ZOS247,DISP=OLD
//SYSIN    DD *

  /* -------------------------------------------------------------- */
  /* ALLOCATE A PAGE DATA SET                                       */
  /* -------------------------------------------------------------- */

    LISTCAT -
        ENTRY(Z24MST.PAGE.CPAC.COMMON)

  IF LASTCC GT 0 THEN DO
    DEFINE PAGESPACE( -
        FILE(PAGECOMM) -
        NAME(Z24MST.PAGE.CPAC.COMMON) -
        TRACKS(808) -
        VOLUME(ZOS247) )

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

  /* -------------------------------------------------------------- */
  /* ALLOCATE A PAGE DATA SET                                       */
  /* -------------------------------------------------------------- */

    LISTCAT -
        ENTRY(Z24MST.PAGE.CPAC.LOCAL1)

  IF LASTCC GT 0 THEN DO
    DEFINE PAGESPACE( -
        FILE(PAGELOC1) -
        NAME(Z24MST.PAGE.CPAC.LOCAL1) -
        TRACKS(34501) -
        VOLUME(ZOS247) )

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

  /* -------------------------------------------------------------- */
  /* ALLOCATE PLPA PAGE DATA SET                                    */
  /* -------------------------------------------------------------- */

    LISTCAT -
        ENTRY(Z24MST.PAGE.CPAC.PLPA)

  IF LASTCC GT 0 THEN DO
    DEFINE PAGESPACE( -
        FILE(PAGEPLPA) -
        NAME(Z24MST.PAGE.CPAC.PLPA) -
        TRACKS(19500) -
        VOLUME(ZOS247) )

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

  /* -------------------------------------------------------------- */
  /* ALLOCATE SMS COMMDS dataset                                    */
  /* -------------------------------------------------------------- */

    LISTCAT -
        ENTRY(Z24MST.SYS1.DFSMS.COMMDS)

  IF LASTCC GT 0 THEN DO
    DEFINE   CLUSTER(                       -
        NAME(Z24MST.SYS1.DFSMS.COMMDS) -
        LINEAR                       -
        REUSE                        -
        VOLUMES(ZOS247) -
        TRACKS(81,9) -
        SHAREOPTIONS(3,3)  )

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

  /* -------------------------------------------------------------- */
  /* ALLOCATE IODF dataset                                          */
  /* -------------------------------------------------------------- */

    LISTCAT -
        ENTRY(Z24MST.SYS1.IODF00)

  IF LASTCC GT 0 THEN DO
    DEFINE CLUSTER( -
        NAME(Z24MST.SYS1.IODF00.CLUSTER) -
        LINEAR -
        TRACKS(146) -
        VOLUMES(ZOS247) -
        ) +
        DATA (NAME (Z24MST.SYS1.IODF00))

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

  /* -------------------------------------------------------------- */
  /* ALLOCATE FIRST SMF DATA SET                                    */
  /* -------------------------------------------------------------- */

    LISTCAT -
        ENTRY(Z24MST.SYS1.MAN1)

  IF LASTCC GT 0 THEN DO
    DEFINE CLUSTER( -
        CONTROLINTERVALSIZE(4096) -
        TRACKS(647) -
        NAME(Z24MST.SYS1.MAN1) -
        NONINDEXED -
        RECORDSIZE(4086,32767) -
        REUSE -
        SHAREOPTIONS(2) -
        SPANNED -
        SPEED -
        VOLUME(ZOS247) )

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

  /* -------------------------------------------------------------- */
  /* ALLOCATE SECOND SMF DATA SET                                   */
  /* -------------------------------------------------------------- */

    LISTCAT -
        ENTRY(Z24MST.SYS1.MAN2)

  IF LASTCC GT 0 THEN DO
    DEFINE CLUSTER( -
        CONTROLINTERVALSIZE(4096) -
        TRACKS(162) -
        NAME(Z24MST.SYS1.MAN2) -
        NONINDEXED -
        RECORDSIZE(4086,32767) -
        REUSE -
        SHAREOPTIONS(2) -
        SPANNED -
        SPEED -
        VOLUME(ZOS247) )

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

  /* -------------------------------------------------------------- */
  /* ALLOCATE THIRD SMF DATA SET                                    */
  /* -------------------------------------------------------------- */

    LISTCAT -
        ENTRY(Z24MST.SYS1.MAN3)

  IF LASTCC GT 0 THEN DO
    DEFINE CLUSTER( -
        CONTROLINTERVALSIZE(4096) -
        TRACKS(162) -
        NAME(Z24MST.SYS1.MAN3) -
        NONINDEXED -
        RECORDSIZE(4086,32767) -
        REUSE -
        SHAREOPTIONS(2) -
        SPANNED -
        SPEED -
        VOLUME(ZOS247) )

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

  /* -------------------------------------------------------------- */
  /* ALLOCATE SMP/E CSI's                                           */
  /* -------------------------------------------------------------- */
    LISTCAT -
        ENTRY(Z24SUP.SMPE.GLOBAL.CSI)

  IF LASTCC GT 0 THEN  DO
    DEFINE CLUSTER( -
        NAME(Z24SUP.SMPE.GLOBAL.CSI) -
        TRACKS(135 14) -
        FREESPACE(0 7)  -
        KEYS(24 0)         -
        RECORDSIZE(24 143)  -
        SHAREOPTIONS(2 3)-
        VOLUMES(ZOS241) -
       ) -
      DATA(CONTROLINTERVALSIZE(8192)) -
      INDEX(CONTROLINTERVALSIZE(4096))

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

  /* -------------------------------------------------------------- */
  /* ALLOCATE SMP/E CSI's                                           */
  /* -------------------------------------------------------------- */
    LISTCAT -
        ENTRY(Z24SUP.SMPE.MVS.DLIB.CSI)

  IF LASTCC GT 0 THEN  DO
    DEFINE CLUSTER( -
        NAME(Z24SUP.SMPE.MVS.DLIB.CSI) -
        TRACKS(1161 117) -
        FREESPACE(0 7)  -
        KEYS(24 0)         -
        RECORDSIZE(24 143)  -
        SHAREOPTIONS(2 3)-
        VOLUMES(ZOS245) -
       ) -
      DATA(CONTROLINTERVALSIZE(8192)) -
      INDEX(CONTROLINTERVALSIZE(4096))

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

  /* -------------------------------------------------------------- */
  /* ALLOCATE SMP/E CSI's                                           */
  /* -------------------------------------------------------------- */
    LISTCAT -
        ENTRY(Z24SUP.SMPE.MVS.TARGET.CSI)

  IF LASTCC GT 0 THEN  DO
    DEFINE CLUSTER( -
        NAME(Z24SUP.SMPE.MVS.TARGET.CSI) -
        TRACKS(1180 118) -
        FREESPACE(0 7)  -
        KEYS(24 0)         -
        RECORDSIZE(24 143)  -
        SHAREOPTIONS(2 3)-
        VOLUMES(ZOS241) -
       ) -
      DATA(CONTROLINTERVALSIZE(8192)) -
      INDEX(CONTROLINTERVALSIZE(4096))

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

  /* -------------------------------------------------------------- */
  /* ALLOCATE SMS ACDS dataset                                      */
  /* -------------------------------------------------------------- */

    LISTCAT -
        ENTRY(Z24MST.SYS1.DFSMS.ACDS)

  IF LASTCC GT 0 THEN DO
    DEFINE   CLUSTER(                      -
        NAME(Z24MST.SYS1.DFSMS.ACDS)     -
        LINEAR                      -
        REUSE                       -
        VOLUMES(ZOS247) -
        TRACKS(1,1) -
        SHAREOPTIONS(3,3)  )

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

  /* -------------------------------------------------------------- */
  /* ALLOCATE SMS SCDS dataset                                      */
  /* -------------------------------------------------------------- */

    LISTCAT -
        ENTRY(Z24MST.SYS1.DFSMS.SCDS)

  IF LASTCC GT 0 THEN DO
    DEFINE   CLUSTER(               -
        NAME(Z24MST.SYS1.DFSMS.SCDS)        -
        LINEAR               -
        REUSE                -
        VOLUMES(ZOS247) -
        TRACKS(1,1) -
        SHAREOPTIONS(2,3)  )

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL
/*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ALCVSAM),(4000,LT))
//*
//*************************************************************
//*   ALL NON-VSAM DATA SETS ARE NOW ALLOCATED.
//*   PLEASE NOTE THAT THE IPLPARM DATA SET IS PLACED ON THE SAME
//*   VOLUME AS THE IODF DATA SET.
//*************************************************************
//*
//*************************************************************
//*   THE FOLLOWING STEP MAKES SURE THAT THE *ONLY* POSSIBLE REASON
//*   FOR A NONZERO RETURN CODE FROM LISTCAT IS "DATA SET NOT FOUND".
//*************************************************************
//*
//CHKDSN3 EXEC PGM=IKJEFT1B,COND=(4000,LT)
//SYSEXEC  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPCENU
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQEXEC
//         DD DISP=SHR,DSN=AOP.SAOPEXEC
//         DD DISP=SHR,DSN=EUVF.SEUVFEXC
//         DD DISP=SHR,DSN=SYS1.SEDGEXE1
//         DD DISP=SHR,DSN=EOY.SEOYCLIB
//         DD DISP=SHR,DSN=EOX.SEPHCLB1
//         DD DISP=SHR,DSN=ISP.SISPEXEC
//         DD DISP=SHR,DSN=IOE.SIOEEXEC
//         DD DISP=SHR,DSN=SYS1.SBPXEXEC
//         DD DISP=SHR,DSN=ISF.SISFEXEC
//         DD DISP=SHR,DSN=GLD.SGLDEXEC
//         DD DISP=SHR,DSN=FFST.V120ESA.SEPWSRC1
//SYSPROC  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPCENU
//         DD DISP=SHR,DSN=CPAC.CMDPROC
//         DD DISP=SHR,DSN=SYS1.SBLSCLI0
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJCLST
//         DD DISP=SHR,DSN=CBC.SCCNUTL
//         DD DISP=SHR,DSN=SYS1.DGTCLIB
//         DD DISP=SHR,DSN=CSF.SCSFCLI0
//         DD DISP=SHR,DSN=EOY.SEOYCLIB
//         DD DISP=SHR,DSN=EOX.SEPHCLB1
//         DD DISP=SHR,DSN=SYS1.SERBCLS
//         DD DISP=SHR,DSN=ISP.SISPCLIB
//         DD DISP=SHR,DSN=SYS1.HRFCLST
//         DD DISP=SHR,DSN=SYS1.SBDTCLI0
//         DD DISP=SHR,DSN=GIM.SGIMCLS0
//         DD DISP=SHR,DSN=ICQ.ICQCCLIB
//         DD DISP=SHR,DSN=SYS1.SCBDCLST
//         DD DISP=SHR,DSN=GDDM.SADMSAM
//         DD DISP=SHR,DSN=FFST.V120ESA.SEPWCENU
//ISPMLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPMENU
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJMLIB
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQMSGE
//         DD DISP=SHR,DSN=TCPIP.SEZAMENU
//         DD DISP=SHR,DSN=AOP.SAOPMENU
//         DD DISP=SHR,DSN=SYS1.DGTMLIB
//         DD DISP=SHR,DSN=SYS1.DFQMLIB
//         DD DISP=SHR,DSN=SYS1.SEDGMENU
//         DD DISP=SHR,DSN=CSF.SCSFMSG0
//         DD DISP=SHR,DSN=EOY.SEOYBENU
//         DD DISP=SHR,DSN=EOY.SEOYMENU
//         DD DISP=SHR,DSN=EOX.SEPHMSG1
//         DD DISP=SHR,DSN=SYS1.SERBMENU
//         DD DISP=SHR,DSN=ISP.SISPMENU
//         DD DISP=SHR,DSN=SYS1.HRFMSG
//         DD DISP=SHR,DSN=SYS1.SBDTMSG
//         DD DISP=SHR,DSN=SYS1.SBLSMSG0
//         DD DISP=SHR,DSN=SYS1.SBPXMENU
//         DD DISP=SHR,DSN=ISF.SISFMLIB
//         DD DISP=SHR,DSN=GIM.SGIMMENU
//         DD DISP=SHR,DSN=ICQ.ICQMLIB
//         DD DISP=SHR,DSN=SYS1.SCBDMENU
//         DD DISP=SHR,DSN=GDDM.SADMMSG
//ISPPLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPPENU
//         DD DISP=SHR,DSN=CPAC.ISPPLIB
//         DD DISP=SHR,DSN=ISP.SISPPENU
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJPLIB
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQPNLE
//         DD DISP=SHR,DSN=TCPIP.SEZAPENU
//         DD DISP=SHR,DSN=SYS1.SBLSPNL0
//         DD DISP=SHR,DSN=AOP.SAOPPENU
//         DD DISP=SHR,DSN=SYS1.DFQPLIB
//         DD DISP=SHR,DSN=SYS1.DGTPLIB
//         DD DISP=SHR,DSN=SYS1.SEDGPENU
//         DD DISP=SHR,DSN=CSF.SCSFPNL0
//         DD DISP=SHR,DSN=EOY.SEOYPENU
//         DD DISP=SHR,DSN=EOX.SEPHPNL1
//         DD DISP=SHR,DSN=SYS1.SERBPENU
//         DD DISP=SHR,DSN=SYS1.HRFPANL
//         DD DISP=SHR,DSN=SYS1.SBDTPNL0
//         DD DISP=SHR,DSN=SYS1.SHASPNL0
//         DD DISP=SHR,DSN=SYS1.SBPXPENU
//         DD DISP=SHR,DSN=ISF.SISFPLIB
//         DD DISP=SHR,DSN=GIM.SGIMPENU
//         DD DISP=SHR,DSN=ICQ.ICQPLIB
//         DD DISP=SHR,DSN=SYS1.SCBDPENU
//         DD DISP=SHR,DSN=GDDM.SADMPNL
//         DD DISP=SHR,DSN=FFST.V120ESA.SEPWPENU
//ISPSLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPSENU
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJSLIB
//         DD DISP=SHR,DSN=SYS1.DGTSLIB
//         DD DISP=SHR,DSN=CSF.SCSFSKL0
//         DD DISP=SHR,DSN=ISP.SISPSLIB
//         DD DISP=SHR,DSN=ISP.SISPSENU
//         DD DISP=SHR,DSN=SYS1.HRFSKEL
//         DD DISP=SHR,DSN=SYS1.SBLSKEL0
//         DD DISP=SHR,DSN=ISF.SISFSLIB
//         DD DISP=SHR,DSN=GIM.SGIMSENU
//         DD DISP=SHR,DSN=ICQ.ICQSLIB
//ISPLLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPLOAD
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJMOD1
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQANLE
//         DD DISP=SHR,DSN=SYS1.DFQLLIB
//         DD DISP=SHR,DSN=SYS1.DGTLLIB
//ISPTLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPTENU
//         DD DISP=SHR,DSN=E002884.ISPF.ISPPROF
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJTLIB
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQTBLE
//         DD DISP=SHR,DSN=SYS1.DGTTLIB
//         DD DISP=SHR,DSN=CSF.SCSFTLIB
//         DD DISP=SHR,DSN=EOY.SEOYTENU
//         DD DISP=SHR,DSN=EOX.SEPHTBL1
//         DD DISP=SHR,DSN=SYS1.SERBT
//         DD DISP=SHR,DSN=SYS1.SERBTENU
//         DD DISP=SHR,DSN=ISP.SISPTENU
//         DD DISP=SHR,DSN=SYS1.SBLSTBL0
//         DD DISP=SHR,DSN=SYS1.SBPXTENU
//         DD DISP=SHR,DSN=ISF.SISFTLIB
//         DD DISP=SHR,DSN=GIM.SGIMTENU
//         DD DISP=SHR,DSN=ICQ.ICQTABLS
//         DD DISP=SHR,DSN=SYS1.SCBDTENU
//ISPLOG   DD DSN=&ISPLOG,
//            DISP=NEW,UNIT=SYSALLDA,SPACE=(CYL,(1,1,1)),
//            DCB=(RECFM=FB,BLKSIZE=3120,LRECL=80)
//ISPPROF  DD DSN=&ISPPROF,
//            DISP=NEW,UNIT=SYSALLDA,SPACE=(CYL,(1,1,1)),
//            DCB=(RECFM=FB,BLKSIZE=3120,LRECL=80)
//SYSPRINT DD SYSOUT=*
//SYSTSPRT DD SYSOUT=*
//SYSTSIN  DD *
 PROFILE PREFIX(E002884)
 ISPSTART CMD(%CPPEDSN CHECKRC F )
/*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKDSN3),(4000,LT))
//*
//*
//*************************************************************
//*
//*  THIS IS TO CHECK WHETHER SMS IS ACTIVE IN THE DRIVING
//*  SYSTEM. IT USES THE REXX MVSVAR FUNCTION TO CHECK IF SMS
//*  IS ACTIVE AND RETURN THE CORRESPONDING RETURN CODE.
//*
//*  IF SMS
//*       ACTIVE   ---    RETURN 0
//*     INACTIVE   ---    RETURN 4
//*  UNAVAILABLE   ---    RETURN 8
//*    ALL OTHER   ---    RETURN 12
//*
//*************************************************************
//CHKSMS5 EXEC PGM=IKJEFT1B
//SYSEXEC  DD DSN=ZOS24.OS241147.ORDER.SCPPCENU,DISP=SHR
//SYSTSPRT DD SYSOUT=*
//SYSTSIN  DD *
  %CPPESMS NOISPF
/*
//*
//NOTOK EXEC  PGM=CPPMAXRC,COND=((0,GE,CHKSMS5),(4000,LT))
//*
//ALLOC1 EXEC PGM=IDCAMS,DYNAMNBR=1010,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *

    LISTCAT -
      ENTRY(Z24SUP.BDT1.BDTSPOOL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BDT1.BDTSPOOL')          -
       NEW                         -
       CATALOG                     -
       RECFM(F   ) -
       LRECL(2048)           -
       DSORG(PS)           -
       BLKSIZE(2048)              -
       AVBLOCK(2048)         -
       TRACKS SPACE(8 0) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BDT1.MSG0001)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BDT1.MSG0001')          -
       NEW                         -
       CATALOG                     -
       RECFM(F   ) -
       LRECL(6156)           -
       DSORG(PS)           -
       BLKSIZE(6156)              -
       AVBLOCK(6156)         -
       TRACKS SPACE(13 2) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BDT1.TQIBITS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BDT1.TQIBITS')          -
       NEW                         -
       CATALOG                     -
       RECFM(F   ) -
       LRECL(31)           -
       DSORG(PS)           -
       BLKSIZE(31)              -
       AVBLOCK(31)         -
       TRACKS SPACE(2 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BDT1.TQIDATA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BDT1.TQIDATA')          -
       NEW                         -
       CATALOG                     -
       RECFM(F   ) -
       LRECL(3584)           -
       DSORG(PS)           -
       BLKSIZE(3584)              -
       AVBLOCK(3584)         -
       TRACKS SPACE(12 0) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.FFST.V120ESA.EPWCKPT)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.FFST.V120ESA.EPWCKPT')          -
       NEW                         -
       CATALOG                     -
       RECFM(F B  ) -
       LRECL(6160)           -
       DSORG(PS)           -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(13 2) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.FFST.V120ESA.EPWDUMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.FFST.V120ESA.EPWDUMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(4160)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(4160)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.FFST.V120ESA.EPWLOG1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.FFST.V120ESA.EPWLOG1')          -
       NEW                         -
       CATALOG                     -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PS)           -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(57 6) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.FFST.V120ESA.EPWLOG2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.FFST.V120ESA.EPWLOG2')          -
       NEW                         -
       CATALOG                     -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PS)           -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(57 6) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.FFST.V120ESA.EPWPARM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.FFST.V120ESA.EPWPARM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.MVSD100.SMPDLOG)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.MVSD100.SMPDLOG')          -
       NEW                         -
       CATALOG                     -
       RECFM(V B  ) -
       LRECL(510)           -
       DSORG(PS)           -
       BLKSIZE(0)              -
       AVBLOCK(6120)         -
       TRACKS SPACE(83 9) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.MVSD100.SMPDLOGA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.MVSD100.SMPDLOGA')          -
       NEW                         -
       CATALOG                     -
       RECFM(V B  ) -
       LRECL(510)           -
       DSORG(PS)           -
       BLKSIZE(0)              -
       AVBLOCK(6120)         -
       TRACKS SPACE(83 9) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.MVSD110.SMPDLOG)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.MVSD110.SMPDLOG')          -
       NEW                         -
       CATALOG                     -
       RECFM(V B  ) -
       LRECL(510)           -
       DSORG(PS)           -
       BLKSIZE(0)              -
       AVBLOCK(6120)         -
       TRACKS SPACE(13 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.MVSD110.SMPDLOGA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.MVSD110.SMPDLOGA')          -
       NEW                         -
       CATALOG                     -
       RECFM(V B  ) -
       LRECL(510)           -
       DSORG(PS)           -
       BLKSIZE(0)              -
       AVBLOCK(6120)         -
       TRACKS SPACE(13 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.MVST100.SMPLTS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.MVST100.SMPLTS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(600)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.MVST100.SMPMTS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.MVST100.SMPMTS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(3120)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.MVST100.SMPSTS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.MVST100.SMPSTS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(3120)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.MVST100.SMPTLOG)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.MVST100.SMPTLOG')          -
       NEW                         -
       CATALOG                     -
       RECFM(V B  ) -
       LRECL(510)           -
       DSORG(PS)           -
       BLKSIZE(0)              -
       AVBLOCK(6120)         -
       TRACKS SPACE(83 9) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.MVST100.SMPTLOGA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.MVST100.SMPTLOGA')          -
       NEW                         -
       CATALOG                     -
       RECFM(V B  ) -
       LRECL(510)           -
       DSORG(PS)           -
       BLKSIZE(0)              -
       AVBLOCK(6120)         -
       TRACKS SPACE(83 9) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.MVST110.SMPLTS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.MVST110.SMPLTS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(600)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.MVST110.SMPMTS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.MVST110.SMPMTS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(3120)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS242)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.MVST110.SMPSCDS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.MVST110.SMPSCDS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(3120)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.MVST110.SMPSTS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.MVST110.SMPSTS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(3120)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.MVST110.SMPTLOG)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.MVST110.SMPTLOG')          -
       NEW                         -
       CATALOG                     -
       RECFM(V B  ) -
       LRECL(510)           -
       DSORG(PS)           -
       BLKSIZE(0)              -
       AVBLOCK(6120)         -
       TRACKS SPACE(13 2) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.MVST110.SMPTLOGA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.MVST110.SMPTLOGA')          -
       NEW                         -
       CATALOG                     -
       RECFM(V B  ) -
       LRECL(510)           -
       DSORG(PS)           -
       BLKSIZE(0)              -
       AVBLOCK(6120)         -
       TRACKS SPACE(13 2) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.SMPGLOG)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.SMPGLOG')          -
       NEW                         -
       CATALOG                     -
       RECFM(V B  ) -
       LRECL(510)           -
       DSORG(PS)           -
       BLKSIZE(0)              -
       AVBLOCK(6120)         -
       TRACKS SPACE(85 9) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.SMPGLOGA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.SMPGLOGA')          -
       NEW                         -
       CATALOG                     -
       RECFM(V B  ) -
       LRECL(510)           -
       DSORG(PS)           -
       BLKSIZE(0)              -
       AVBLOCK(6120)         -
       TRACKS SPACE(85 9) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.BDT.INITS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.BDT.INITS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(3120)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.CPAC.HZSPDATA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.CPAC.HZSPDATA')          -
       NEW                         -
       CATALOG                     -
       RECFM(F B  ) -
       LRECL(4096)           -
       DSORG(PS)           -
       BLKSIZE(0)              -
       AVBLOCK(24576)         -
       TRACKS SPACE(50 5) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.CPAC.LOGREC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.CPAC.LOGREC')          -
       NEW                         -
       CATALOG                     -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PS)           -
       BLKSIZE(32760)              -
       AVBLOCK(1944)         -
       TRACKS SPACE(59 0) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.DAE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.DAE')          -
       NEW                         -
       CATALOG                     -
       RECFM(F B  ) -
       LRECL(255)           -
       DSORG(PS)           -
       BLKSIZE(0)              -
       AVBLOCK(18870)         -
       TRACKS SPACE(25 3) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.DUMP00)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.DUMP00')          -
       NEW                         -
       CATALOG                     -
       RECFM(F   ) -
       LRECL(4160)           -
       DSORG(PS)           -
       BLKSIZE(4160)              -
       AVBLOCK(4160)         -
       TRACKS SPACE(546 0) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.DUMP01)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.DUMP01')          -
       NEW                         -
       CATALOG                     -
       RECFM(F   ) -
       LRECL(4160)           -
       DSORG(PS)           -
       BLKSIZE(4160)              -
       AVBLOCK(4160)         -
       TRACKS SPACE(546 0) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.DUMP02)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.DUMP02')          -
       NEW                         -
       CATALOG                     -
       RECFM(F   ) -
       LRECL(4160)           -
       DSORG(PS)           -
       BLKSIZE(4160)              -
       AVBLOCK(4160)         -
       TRACKS SPACE(546 0) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.GMJD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.GMJD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(120)             -
       BLKSIZE(0)              -
       AVBLOCK(3120)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.PROCLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.PROCLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.WLM.DEF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.WLM.DEF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(6160)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(21 0) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.AOP.AAOPEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.AOP.AAOPEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.AOP.AAOPHFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.AOP.AAOPHFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(22)             -
       BLKSIZE(0)              -
       AVBLOCK(27998)         -
       TRACKS SPACE(2511 252) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.AOP.AAOPMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.AOP.AAOPMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.AOP.AAOPMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.AOP.AAOPMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(53)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(212 22) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.AOP.AAOPPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.AOP.AAOPPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(38)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(93 10) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.AOP.SAOPEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.AOP.SAOPEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.AOP.SAOPMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.AOP.SAOPMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.AOP.SAOPPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.AOP.SAOPPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(38)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(93 10) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ASM.AASMMAC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ASM.AASMMAC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(15 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ASM.AASMMAC2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ASM.AASMMAC2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ASM.AASMMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ASM.AASMMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(22)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(38 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ASM.AASMMOD2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ASM.AASMMOD2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(95)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(115 12) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ASM.AASMPUT2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ASM.AASMPUT2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(137 14) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ASM.AASMSAM1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ASM.AASMSAM1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(27 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ASM.AASMSAM2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ASM.AASMSAM2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ASM.SASMMAC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ASM.SASMMAC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(15 2) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ASM.SASMMAC2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ASM.SASMMAC2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ASM.SASMMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ASM.SASMMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(37 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ASM.SASMMOD2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ASM.SASMMOD2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(61 7) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ASM.SASMPUT2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ASM.SASMPUT2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(137 14) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ASM.SASMSAM1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ASM.SASMSAM1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(27 3) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ASM.SASMSAM2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ASM.SASMSAM2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.AZD.AAZDFFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.AZD.AAZDFFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(256)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(27998)         -
       TRACKS SPACE(16086 1609) -
       UNIT(3390)            -
       VOLUME(ZOS244)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BAQ.ABAQCOB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BAQ.ABAQCOB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BAQ.ABAQINST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BAQ.ABAQINST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BAQ.ABAQLIB1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BAQ.ABAQLIB1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BAQ.ABAQMOD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BAQ.ABAQMOD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BAQ.ABAQPLI)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BAQ.ABAQPLI')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BAQ.ABAQSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BAQ.ABAQSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(13 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BAQ.ABAQSCR)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BAQ.ABAQSCR')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(V B  ) -
       LRECL(32000)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(32004)         -
       TRACKS SPACE(2 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BAQ.ABAQZFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BAQ.ABAQZFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(V   ) -
       LRECL(32000)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(32004)         -
       TRACKS SPACE(7305 731) -
       UNIT(3390)            -
       VOLUME(ZOS244)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BAQ.SBAQCOB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BAQ.SBAQCOB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BAQ.SBAQINST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BAQ.SBAQINST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BAQ.SBAQLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BAQ.SBAQLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(10 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BAQ.SBAQLIB1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BAQ.SBAQLIB1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BAQ.SBAQPLI)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BAQ.SBAQPLI')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BAQ.SBAQSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BAQ.SBAQSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(13 2) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BBL.ABBLEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BBL.ABBLEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BBL.ABBLJCL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BBL.ABBLJCL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BBL.ABBLLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BBL.ABBLLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(27998)         -
       TRACKS SPACE(10746 1075) -
       UNIT(3390)            -
       VOLUME(ZOS244)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BBL.SBBLEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BBL.SBBLEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.BBL.SBBLJCL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.BBL.SBBLJCL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.ACCNCMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.ACCNCMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(6537 654) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.ACCNSR1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.ACCNSR1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(25)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(75 8) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.ACCNSR2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.ACCNSR2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(11)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(25 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.ACCNSR3)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.ACCNSR3')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(11)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(37 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.ACCNSR4)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.ACCNSR4')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(11)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(25 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.ACCNSR5)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.ACCNSR5')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(16)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(119 12) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.ACCNSR6)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.ACCNSR6')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(25 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.ACCNSR7)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.ACCNSR7')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(17)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(122 13) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.ACCNSR8)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.ACCNSR8')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(25 3) -
       UNIT(3390)            -
       VOLUME(ZOS244)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.ACCNSR9)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.ACCNSR9')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(17)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(125 13) -
       UNIT(3390)            -
       VOLUME(ZOS244)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.ACLBDLL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.ACLBDLL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(115 12) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.ACLBDLL2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.ACLBDLL2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(335 34) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.ACLBHFS1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.ACLBHFS1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6317 632) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.ACLBOBJ)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.ACLBOBJ')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(129 13) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.ACLBSR1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.ACLBSR1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(120)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(20 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.ACTVMOD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.ACTVMOD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.ACTVSRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.ACTVSRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCCNCMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCCNCMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(6537 654) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCCNDOC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCCNDOC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCCNJCL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCCNJCL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCCNM10)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCCNM10')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(11)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(25 3) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCCNM11)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCCNM11')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(11)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(25 3) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCCNM12)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCCNM12')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(25 3) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCCNM13)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCCNM13')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(25 3) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCCNN10)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCCNN10')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(11)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(37 4) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCCNN11)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCCNN11')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(16)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(119 12) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCCNN12)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCCNN12')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(17)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(122 13) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCCNN13)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCCNN13')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(17)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(125 13) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCCNOBJ)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCCNOBJ')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(35 4) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCCNPRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCCNPRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCCNSAM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCCNSAM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(15)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(27 3) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCCNUTL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCCNUTL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCLBCPP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCLBCPP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(88 9) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCLBDLL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCLBDLL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(24 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCLBDLL2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCLBDLL2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(19 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCLBH.H)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCLBH.H')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(120)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(11 2) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCLBJCL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCLBJCL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCLBSID)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCLBSID')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCTVJCL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCTVJCL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CBC.SCTVMOD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CBC.SCTVMOD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(15 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.CDS.ACDSHFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.CDS.ACDSHFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(570 57) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.CDS.ACDSSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.CDS.ACDSSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.CDS.SCDSSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.CDS.SCDSSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.AAFHMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.AAFHMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(607)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(177 17) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.AAFHSRC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.AAFHSRC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(22 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.ACEEMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.ACEEMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(445)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(533 53) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.ACEEMOD2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.ACEEMOD2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(23201 2321) -
       UNIT(3390)            -
       VOLUME(ZOS244)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.ACEESRC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.ACEESRC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(16)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(211 21) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.ACEESRC2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.ACEESRC2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(120)           -
       DSORG(PO)           -
       DIR(13)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(118 12) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.ACEESRC3)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.ACEESRC3')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(120)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(21 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.AEDCCPP1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.AEDCCPP1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(22 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.AEDCHFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.AEDCHFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(97)             -
       BLKSIZE(0)              -
       AVBLOCK(8670)         -
       TRACKS SPACE(4279 428) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.AEDCMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.AEDCMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(1541)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(1486 146) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.AEDCMOD2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.AEDCMOD2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(63)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.AEDCMSEP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.AEDCMSEP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(150)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6150)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.AEDCOBJ1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.AEDCOBJ1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(11 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.AEDCSRC6)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.AEDCSRC6')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(71)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(766 77) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.AEDCSRC7)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.AEDCSRC7')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(256)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(1183 118) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.AIBMMOD3)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.AIBMMOD3')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(529)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(346 34) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.AIBMSRC3)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.AIBMSRC3')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(33 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.AIGZMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.AIGZMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(220)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(122 12) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.AIGZSRC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.AIGZSRC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(37 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SAFHFORT)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SAFHFORT')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(59)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(20 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEEBIND)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEEBIND')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEEBND2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEEBND2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEECICS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEECICS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEECLST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEECLST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(12 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEECMAP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEECMAP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(11)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(524 53) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEECPP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEECPP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(22 3) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEEGXLT)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEEGXLT')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(37)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(140 14) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEEH)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEEH')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(120)           -
       DSORG(PO)           -
       DIR(13)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(118 12) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEEH.ARPA.H)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEEH.ARPA.H')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEEH.H)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEEH.H')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(20)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(160 16) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEEH.NET.H)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEEH.NET.H')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEEH.NETINET.H)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEEH.NETINET.H')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(7 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEEH.SYS.H)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEEH.SYS.H')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(27 3) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEEH.T)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEEH.T')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(120)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(21 3) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEELIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEELIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(80 8) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEELKED)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEELKED')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(2309)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(669 62) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEELKEX)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEELKEX')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(205)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(114 11) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEELOCL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEELOCL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(83 9) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEELOCX)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEELOCX')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(23)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(920 92) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEELPA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEELPA')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(259 26) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEEMAC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEEMAC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(86 9) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEEMSGP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEEMSGP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(150)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6150)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEEOBJ)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEEOBJ')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEEPROC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEEPROC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEERUN)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEERUN')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(784)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(1980 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEERUN2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEERUN2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(23845 2385) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEESAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEESAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(39)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(146 15) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEESPC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEESPC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(67)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(15 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SCEESPCO)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SCEESPCO')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(7 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SIBMAM24)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SIBMAM24')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(20)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SIBMCALL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SIBMCALL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SIBMCAL2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SIBMCAL2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SIBMMATH)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SIBMMATH')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(46)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CEE.SIBMTASK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CEE.SIBMTASK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.CFZ.ACFZHFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.CFZ.ACFZHFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(1020 102) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CPAC.CMDPROC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CPAC.CMDPROC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CPAC.DOCLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CPAC.DOCLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(215 22) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CPAC.ISPPLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CPAC.ISPPLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CPAC.IVPLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CPAC.IVPLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CPAC.LINKLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CPAC.LINKLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(5 0) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CPAC.PARMLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CPAC.PARMLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(151)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(102 10) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CPAC.PDFPD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CPAC.PDFPD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8209)         -
       TRACKS SPACE(526 53) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CPAC.PROCLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CPAC.PROCLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(30)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(19 2) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CPAC.REPORTS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CPAC.REPORTS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B A ) -
       LRECL(125)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(6160)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(974 98) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CPAC.SAMPLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CPAC.SAMPLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CPAC.VTAMLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CPAC.VTAMLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3 0) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CPAC.VTAMLST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CPAC.VTAMLST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(41)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(116 12) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.ACSFCLI0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.ACSFCLI0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(33 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.ACSFHDRS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.ACSFHDRS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.ACSFHFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.ACSFHFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(41 5) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.ACSFMOD0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.ACSFMOD0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(200)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(240 24) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.ACSFMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.ACSFMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.ACSFMSG0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.ACSFMSG0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(11 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.ACSFOBJ)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.ACSFOBJ')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.ACSFPNL0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.ACSFPNL0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(23)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(47 5) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.ACSFSKL0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.ACSFSKL0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.ACSFTLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.ACSFTLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.SCSFCLI0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.SCSFCLI0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(33 4) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.SCSFHDRS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.SCSFHDRS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.SCSFMOD0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.SCSFMOD0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(153)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(206 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.SCSFMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.SCSFMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.SCSFMSG0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.SCSFMSG0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(11 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.SCSFOBJ)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.SCSFOBJ')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.SCSFPNL0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.SCSFPNL0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(23)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(47 5) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.SCSFSKL0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.SCSFSKL0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.SCSFSTUB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.SCSFSTUB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(143)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(60 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.CSF.SCSFTLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.CSF.SCSFTLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.ADGACICS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.ADGACICS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(43 5) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.ADGACNTL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.ADGACNTL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.ADGADATA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.ADGADATA')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(2048)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(23476)         -
       TRACKS SPACE(98 10) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.ADGAISPC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.ADGAISPC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(39 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.ADGAJCL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.ADGAJCL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(11 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.ADGALINK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.ADGALINK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(445 45) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.ADGAMAC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.ADGAMAC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(21)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(90 9) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.ADGAMAP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.ADGAMAP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.ADGAMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.ADGAMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(22)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(11 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.ADGAMIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.ADGAMIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(256)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(23476)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.ADGAMSGS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.ADGAMSGS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.ADGAOPLS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.ADGAOPLS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.ADGAPARM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.ADGAPARM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(7 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.ADGAPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.ADGAPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(94)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(119 12) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.ADGAPROC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.ADGAPROC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(19)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(15 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.ADGAPROF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.ADGAPROF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.ADGASAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.ADGASAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(297 30) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.ADGASENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.ADGASENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.ADGATRP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.ADGATRP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(256)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(23476)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.SDGACICS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.SDGACICS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(43 5) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.SDGACNTL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.SDGACNTL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.SDGADATA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.SDGADATA')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(2048)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(23476)         -
       TRACKS SPACE(98 10) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.SDGAISPC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.SDGAISPC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(39 4) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.SDGAJCL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.SDGAJCL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(11 2) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.SDGALINK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.SDGALINK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(445 45) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.SDGAMAC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.SDGAMAC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(21)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(90 9) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.SDGAMAP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.SDGAMAP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.SDGAMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.SDGAMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(22)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(11 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.SDGAMIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.SDGAMIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(256)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(23476)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.SDGAMSGS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.SDGAMSGS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.SDGAOPLS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.SDGAOPLS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.SDGAPARM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.SDGAPARM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(7 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.SDGAPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.SDGAPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(94)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(119 12) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.SDGAPROC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.SDGAPROC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(19)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(15 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.SDGAPROF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.SDGAPROF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.SDGASAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.SDGASAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(297 30) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.SDGASENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.SDGASENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.DGA.SDGATRP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.DGA.SDGATRP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(256)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(23476)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.EUVF.AEUVFEXC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.EUVF.AEUVFEXC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.EUVF.AEUVFHFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.EUVF.AEUVFHFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(62 7) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.EUVF.AEUVFLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.EUVF.AEUVFLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.EUVF.AEUVFSAM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.EUVF.AEUVFSAM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.EUVF.SEUVFEXC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.EUVF.SEUVFEXC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.EUVF.SEUVFLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.EUVF.SEUVFLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.EUVF.SEUVFSAM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.EUVF.SEUVFSAM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.FFST.V120ESA.AEPWBENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.FFST.V120ESA.AEPWBENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(4096)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(4096)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.FFST.V120ESA.AEPWMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.FFST.V120ESA.AEPWMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(98)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(138 14) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.FFST.V120ESA.AEPWSRC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.FFST.V120ESA.AEPWSRC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(15 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.FFST.V120ESA.SEPWBENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.FFST.V120ESA.SEPWBENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(4096)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(4096)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.FFST.V120ESA.SEPWCENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.FFST.V120ESA.SEPWCENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.FFST.V120ESA.SEPWMAC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.FFST.V120ESA.SEPWMAC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.FFST.V120ESA.SEPWMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.FFST.V120ESA.SEPWMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(15 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.FFST.V120ESA.SEPWMOD2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.FFST.V120ESA.SEPWMOD2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(6 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.FFST.V120ESA.SEPWMOD3)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.FFST.V120ESA.SEPWMOD3')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.FFST.V120ESA.SEPWMOD4)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.FFST.V120ESA.SEPWMOD4')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(64)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(174 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.FFST.V120ESA.SEPWPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.FFST.V120ESA.SEPWPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.FFST.V120ESA.SEPWSRC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.FFST.V120ESA.SEPWSRC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.FFST.V120ESA.SEPWSRC2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.FFST.V120ESA.SEPWSRC2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(7 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.AADMCDA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.AADMCDA')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(400)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.AADMCFO)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.AADMCFO')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(400)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.AADMDAT)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.AADMDAT')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(30 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.AADMGDF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.AADMGDF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(400)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(11 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.AADMIMG)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.AADMIMG')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(400)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.AADMMAP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.AADMMAP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(400)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.AADMMOD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.AADMMOD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(349)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(347 34) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.AADMMSG)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.AADMMSG')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.AADMOPS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.AADMOPS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.AADMPCF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.AADMPCF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(400)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(99 10) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.AADMPNL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.AADMPNL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.AADMSAM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.AADMSAM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(13)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(175 18) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.AADMSYM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.AADMSYM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(400)           -
       DSORG(PO)           -
       DIR(22)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(120 12) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.SADMCDA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.SADMCDA')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(400)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.SADMCFO)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.SADMCFO')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(400)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.SADMDAT)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.SADMDAT')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(30 3) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.SADMGDF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.SADMGDF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(400)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(11 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.SADMIMG)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.SADMIMG')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(400)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.SADMMAP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.SADMMAP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(400)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.SADMMOD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.SADMMOD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(71)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(249 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.SADMMSG)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.SADMMSG')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.SADMOPS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.SADMOPS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.SADMPCF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.SADMPCF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(400)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(99 10) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.SADMPNL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.SADMPNL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.SADMSAM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.SADMSAM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(13)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(175 18) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GDDM.SADMSYM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GDDM.SADMSYM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(400)           -
       DSORG(PO)           -
       DIR(22)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(120 12) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GIM.AGIMBIN)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GIM.AGIMBIN')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GIM.AGIMCLS0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GIM.AGIMCLS0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GIM.AGIMMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GIM.AGIMMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(11 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GIM.AGIMPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GIM.AGIMPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(92)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(154 16) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GIM.AGIMSENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GIM.AGIMSENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(16 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GIM.AGIMTENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GIM.AGIMTENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GIM.SGIMCLS0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GIM.SGIMCLS0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GIM.SGIMLMD0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GIM.SGIMLMD0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(78 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GIM.SGIMMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GIM.SGIMMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(11 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GIM.SGIMPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GIM.SGIMPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(92)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(154 16) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GIM.SGIMSENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GIM.SGIMSENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(16 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.GIM.SGIMTENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.GIM.SGIMTENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.GLD.AGLDEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.GLD.AGLDEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.GLD.AGLDEXPC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.GLD.AGLDEXPC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.GLD.AGLDHCLI)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.GLD.AGLDHCLI')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.GLD.AGLDHDRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.GLD.AGLDHDRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.GLD.AGLDHFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.GLD.AGLDHFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(79 8) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.GLD.AGLDSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.GLD.AGLDSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(15 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.GLD.SGLDEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.GLD.SGLDEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.GLD.SGLDEXPC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.GLD.SGLDEXPC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.GLD.SGLDHDRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.GLD.SGLDHDRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.GLD.SGLDSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.GLD.SGLDSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(15 2) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.GSK.AGSKHFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.GSK.AGSKHFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(18 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.GSK.AGSKSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.GSK.AGSKSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.GSK.SGSKSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.GSK.SGSKSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.HAP.AHAPEXE3)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.HAP.AHAPEXE3')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(27998)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.HAP.AHAPINC3)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.HAP.AHAPINC3')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(27998)         -
       TRACKS SPACE(343 35) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.HAP.AHAPJCL3)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.HAP.AHAPJCL3')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(27920)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.HAP.SHAPEXE3)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.HAP.SHAPEXE3')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(27998)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.HAP.SHAPJCL3)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.HAP.SHAPJCL3')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(27920)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.HKC.AHKCKC4Z)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.HKC.AHKCKC4Z')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(259)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(27998)         -
       TRACKS SPACE(507 51) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.IBMZ.AIBMZHFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.IBMZ.AIBMZHFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(27998)         -
       TRACKS SPACE(26 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.IBMZ.AIBMZMOD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.IBMZ.AIBMZMOD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(1398 140) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.IBMZ.AIBMZSRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.IBMZ.AIBMZSRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(26 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.IBMZ.SIBMZCMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.IBMZ.SIBMZCMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(1398 140) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.IBMZ.SIBMZPRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.IBMZ.SIBMZPRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.IBMZ.SIBMZSAM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.IBMZ.SIBMZSAM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(23 3) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.ICQ.ICQABTXT)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.ICQ.ICQABTXT')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.ICQ.ICQCCLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.ICQ.ICQCCLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(169 17) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.ICQ.ICQILIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.ICQ.ICQILIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.ICQ.ICQMLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.ICQ.ICQMLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(16)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(19 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.ICQ.ICQPLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.ICQ.ICQPLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(223)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(343 34) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.ICQ.ICQSLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.ICQ.ICQSLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.ICQ.ICQTABLS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.ICQ.ICQTABLS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.IGY.AIGYHFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.IGY.AIGYHFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(27998)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.IGY.AIGYMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.IGY.AIGYMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(2352 236) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.IGY.AIGYSRC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.IGY.AIGYSRC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(36 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.IGY.SIGYCOMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.IGY.SIGYCOMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(2355 236) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.IGY.SIGYMAC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.IGY.SIGYMAC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.IGY.SIGYPROC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.IGY.SIGYPROC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.IGY.SIGYSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.IGY.SIGYSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(28 3) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IOE.AIOEEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IOE.AIOEEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IOE.AIOEHLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IOE.AIOEHLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IOE.AIOEPROC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IOE.AIOEPROC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IOE.AIOESAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IOE.AIOESAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IOE.SIOEEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IOE.SIOEEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IOE.SIOEPROC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IOE.SIOEPROC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IOE.SIOESAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IOE.SIOESAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.AISFEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.AISFEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(18 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.AISFHELP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.AISFHELP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.AISFHFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.AISFHFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(146 15) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.AISFJCL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.AISFJCL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(15 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.AISFLOAD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.AISFLOAD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(141)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(288 29) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.AISFMAC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.AISFMAC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(43 5) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.AISFMLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.AISFMLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.AISFPLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.AISFPLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(254)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(469 47) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.AISFSLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.AISFSLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.AISFSRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.AISFSRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.AISFTLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.AISFTLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(111 12) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.SISFEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.SISFEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(18 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.SISFHELP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.SISFHELP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.SISFJCL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.SISFJCL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(15 2) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.SISFLINK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.SISFLINK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(5 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.SISFLOAD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.SISFLOAD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(19)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(238 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.SISFLPA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.SISFLPA')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(51 6) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.SISFMAC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.SISFMAC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(43 5) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.SISFMIG)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.SISFMIG')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(32)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.SISFMLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.SISFMLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.SISFPLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.SISFPLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(254)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(469 47) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.SISFSLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.SISFSLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.SISFSRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.SISFSRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISF.SISFTLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISF.SISFTLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(111 12) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.AISPALIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.AISPALIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.AISPCLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.AISPCLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(22 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.AISPEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.AISPEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(111 12) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.AISPGENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.AISPGENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(211)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(290 29) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.AISPGMLI)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.AISPGMLI')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(82)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(179 18) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.AISPGUI)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.AISPGUI')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(325 33) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.AISPHELP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.AISPHELP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.AISPMACS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.AISPMACS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(13)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(61 7) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.AISPMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.AISPMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(53)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(50 5) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.AISPMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.AISPMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(471)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(604 60) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.AISPPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.AISPPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(321)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(505 50) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.AISPSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.AISPSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(22)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(104 11) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.AISPSENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.AISPSENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(79)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(109 11) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.AISPSLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.AISPSLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(29 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.AISPTENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.AISPTENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.SISPALIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.SISPALIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.SISPCLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.SISPCLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(22 3) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.SISPEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.SISPEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(111 12) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.SISPGENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.SISPGENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(211)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(290 29) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.SISPGMLI)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.SISPGMLI')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(82)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(179 18) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.SISPGUI)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.SISPGUI')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(325 33) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.SISPHELP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.SISPHELP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.SISPLOAD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.SISPLOAD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(63)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(152 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.SISPLPA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.SISPLPA')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(40)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(474 48) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.SISPMACS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.SISPMACS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(13)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(61 7) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.SISPMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.SISPMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(53)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(50 5) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.SISPPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.SISPPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(321)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(505 50) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.SISPSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.SISPSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(22)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(102 11) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.SISPSENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.SISPSENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(79)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(109 11) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.SISPSLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.SISPSLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(29 3) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.ISP.SISPTENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.ISP.SISPTENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.IXM.AIXMEXP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.IXM.AIXMEXP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(118 12) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.IXM.AIXMHFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.IXM.AIXMHFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(256)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(4889 489) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.IXM.AIXMLOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.IXM.AIXMLOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(7026 703) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.IXM.SIXMEXP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.IXM.SIXMEXP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(118 12) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.IXM.SIXMLOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.IXM.SIXMLOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(7026 703) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IZD.AIZDEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IZD.AIZDEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IZD.AIZDINST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IZD.AIZDINST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IZD.AIZDLOAD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IZD.AIZDLOAD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(14)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(87 9) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IZD.AIZDMAPS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IZD.AIZDMAPS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(1024)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(10244)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IZD.AIZDMESG)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IZD.AIZDMESG')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IZD.AIZDSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IZD.AIZDSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(33 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IZD.SIZDEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IZD.SIZDEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IZD.SIZDINST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IZD.SIZDINST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IZD.SIZDLOAD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IZD.SIZDLOAD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(14)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(87 9) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IZD.SIZDMAPS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IZD.SIZDMAPS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(1024)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(10244)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IZD.SIZDMESG)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IZD.SIZDMESG')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IZD.SIZDSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IZD.SIZDSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(33 4) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.IZU.AIZUFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.IZU.AIZUFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(V B  ) -
       LRECL(259)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5653 566) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.JAVA.AAJVHFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.JAVA.AAJVHFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(256)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(10674 1068) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.JAVA.AAJVSMP1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.JAVA.AAJVSMP1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.JAVA.SAJVSMP1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.JAVA.SAJVSMP1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQASMS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQASMS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(11 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQCLST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQCLST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQCOBC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQCOBC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(34 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQCOBS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQCOBS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(49 5) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQCPPS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQCPPS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(2 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQC37S)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQC37S')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(59 6) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQC370)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQC370')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(68 7) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQDEFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQDEFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(56 6) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(23 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQHPPS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQHPPS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(7 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQINST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQINST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(12 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQMACS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQMACS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(59 6) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQMAPS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQMAPS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQMOD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQMOD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(1957 196) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQMSGC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQMSGC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQMSGE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQMSGE')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQMSGF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQMSGF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQMSGK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQMSGK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQMSGU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQMSGU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQOJSH)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQOJSH')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(V B  ) -
       LRECL(130)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(27998)         -
       TRACKS SPACE(2 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQOJTR)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQOJTR')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(V B  ) -
       LRECL(32000)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(32004)         -
       TRACKS SPACE(2262 227) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQOWPX)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQOWPX')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(V B  ) -
       LRECL(32000)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(32004)         -
       TRACKS SPACE(6156 616) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQOWSH)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQOWSH')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(V B  ) -
       LRECL(130)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(27998)         -
       TRACKS SPACE(2 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQPLIC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQPLIC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(31 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQPLIS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQPLIS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(2 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQPNLA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQPNLA')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQPNLC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQPNLC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(96 10) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQPNLE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQPNLE')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(107 11) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQPNLF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQPNLF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(115 12) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQPNLK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQPNLK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(108 11) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQPNLU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQPNLU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(107 11) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQPROC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQPROC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(45 5) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQTBLC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQTBLC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQTBLE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQTBLE')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQTBLF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQTBLF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQTBLK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQTBLK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.ACSQTBLU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.ACSQTBLU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQANLC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQANLC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(18 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQANLE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQANLE')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(18 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQANLF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQANLF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(17 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQANLK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQANLK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(19 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQANLU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQANLU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(18 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQASMS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQASMS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(11 1) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQAUTH)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQAUTH')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(1469 147) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQCICS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQCICS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(61 7) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQCLST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQCLST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQCOBC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQCOBC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(34 4) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQCOBS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQCOBS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(49 5) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQCPPS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQCPPS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(2 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQC37S)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQC37S')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(59 6) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQC370)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQC370')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(68 7) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQDEFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQDEFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(56 6) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(23 3) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQHPPS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQHPPS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(7 1) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQINST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQINST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(12 2) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQLINK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQLINK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQLOAD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQLOAD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(190 19) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQMACS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQMACS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(59 6) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQMAPS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQMAPS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQMSGC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQMSGC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQMSGE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQMSGE')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQMSGF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQMSGF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQMSGK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQMSGK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQMSGU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQMSGU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQMVR1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQMVR1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(339 34) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQPLIC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQPLIC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(31 3) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQPLIS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQPLIS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(2 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQPNLA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQPNLA')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQPNLC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQPNLC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(96 10) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQPNLE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQPNLE')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(107 11) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQPNLF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQPNLF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(115 12) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQPNLK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQPNLK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(108 11) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQPNLU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQPNLU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(107 11) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQPROC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQPROC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(45 5) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQSNLC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQSNLC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQSNLE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQSNLE')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQSNLF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQSNLF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQSNLK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQSNLK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQSNLU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQSNLU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQTBLC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQTBLC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQTBLE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQTBLE')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQTBLF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQTBLF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQTBLK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQTBLK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.MQ.V9R1MX.SCSQTBLU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.MQ.V9R1MX.SCSQTBLU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(1 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.AAQNCLST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.AAQNCLST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.AAQNLINK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.AAQNLINK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.AAQNPARM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.AAQNPARM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.AAQNSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.AAQNSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.ABNJPNL1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.ABNJPNL1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(280)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(445 44) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.ABNJPNL2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.ABNJPNL2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(12 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.ABNJSRC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.ABNJSRC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(16 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.ACNMCLST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.ACNMCLST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(69)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(986 99) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.ACNMINST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.ACNMINST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.ACNMLINK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.ACNMLINK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(1000)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(1416 140) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.ACNMMAC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.ACNMMAC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(16)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(153 16) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.ACNMPLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.ACNMPLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.ACNMPNL1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.ACNMPNL1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(206)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(806 81) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.ACNMSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.ACNMSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(41)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(398 40) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.ACNMUXCL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.ACNMUXCL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(516)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8256)         -
       TRACKS SPACE(124 13) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.ADSIMSG1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.ADSIMSG1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.ADSIOPEN)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.ADSIOPEN')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.ADSIPARM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.ADSIPARM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(17)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(115 12) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.ADSIPRF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.ADSIPRF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.ADUIMSG1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.ADUIMSG1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.AEJNUNIX)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.AEJNUNIX')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(2333)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(27998)         -
       TRACKS SPACE(806 81) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.AEKGLANG)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.AEKGLANG')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(125)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(3125)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.AFLBDAT1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.AFLBDAT1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(1028)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8256)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.ANVULIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.ANVULIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(7 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.BNJPNL1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.BNJPNL1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(280)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(445 44) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.BNJPNL2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.BNJPNL2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(12 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.BNJSRC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.BNJSRC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(16 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.CNMCLST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.CNMCLST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(69)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(1084 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.CNMINST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.CNMINST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.CNMLINK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.CNMLINK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(251)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(1259 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.CNMPNL1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.CNMPNL1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(206)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(806 81) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.CNMSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.CNMSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(41)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(398 40) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.DSIPARM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.DSIPARM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(17)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(126 0) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.DSIPRF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.DSIPRF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.NVULIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.NVULIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(7 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.SAQNCLST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.SAQNCLST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.SAQNLINK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.SAQNLINK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.SAQNPARM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.SAQNPARM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.SAQNSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.SAQNSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.SCNMLNKN)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.SCNMLNKN')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(5 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.SCNMLNK1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.SCNMLNK1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(4 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.SCNMLPA1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.SCNMLPA1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(7 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.SCNMMAC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.SCNMMAC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(16)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(153 16) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.SCNMPLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.SCNMPLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.SCNMUXCL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.SCNMUXCL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(516)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8256)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.SDSIMSG1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.SDSIMSG1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.SDSIOPEN)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.SDSIOPEN')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.SDUIMSG1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.SDUIMSG1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.SEKGLANG)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.SEKGLANG')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(125)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(3125)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.NETVIEW.SFLBDAT1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.NETVIEW.SFLBDAT1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(1028)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8256)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.REXX.AEAGJENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.REXX.AEAGJENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.REXX.AEAGMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.REXX.AEAGMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.REXX.AEAGMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.REXX.AEAGMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.REXX.AEAGSAM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.REXX.AEAGSAM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.REXX.SEAGALT)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.REXX.SEAGALT')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(4 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.REXX.SEAGJENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.REXX.SEAGJENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.REXX.SEAGMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.REXX.SEAGMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.REXX.SEAGSAM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.REXX.SEAGSAM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.MVST100.SMPSCDS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.MVST100.SMPSCDS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(152)             -
       BLKSIZE(0)              -
       AVBLOCK(3120)         -
       TRACKS SPACE(76 8) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.SMPPTS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.SMPPTS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(700)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(74129 7413) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.SMPE.SMPPTS1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.SMPE.SMPPTS1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(700)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(57873 5788) -
       UNIT(3390)            -
       VOLUME(ZOS242)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AACBCNTL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AACBCNTL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(32 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AADFMAC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AADFMAC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AADRLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AADRLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(76)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(118 12) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AADRYLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AADRYLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AAXREXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AAXREXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(21 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABDTCLI0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABDTCLI0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABDTHELP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABDTHELP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABDTMAC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABDTMAC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(20)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(129 13) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABDTMOD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABDTMOD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(39)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(39 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABDTMSG)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABDTMSG')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABDTPNL0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABDTPNL0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABDTSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABDTSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABLSCLI0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABLSCLI0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(20)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(355 36) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABLSKEL0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABLSKEL0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(12 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABLSMSG0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABLSMSG0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(13 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABLSPNL0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABLSPNL0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(245)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(414 41) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABLSTBL0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABLSTBL0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABMFMOD0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABMFMOD0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(23)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(36 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABPXEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABPXEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(47 5) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABPXMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABPXMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(13 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABPXMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABPXMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(181)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(362 36) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABPXPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABPXPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(28)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(37 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABPXSPC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABPXSPC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABPXTENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABPXTENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ABPXXMLS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ABPXXMLS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(256)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACBDCLST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACBDCLST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACBDHENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACBDHENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(553)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(470 46) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACBDMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACBDMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(82)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(30 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACBDMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACBDMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(123)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(371 37) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACBDMOD2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACBDMOD2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(7 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACBDPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACBDPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(119)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(92 9) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACBDTEMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACBDTEMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(23 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACBDTENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACBDTENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACBRDBRM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACBRDBRM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(12 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACBRMOD0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACBRMOD0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(153)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(171 17) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACCRHFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACCRHFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(256)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACCRMOD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACCRMOD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(21)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(29 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACDMMOD0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACDMMOD0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(34)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(59 6) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACDRMODS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACDRMODS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(11 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACDRTABL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACDRTABL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(289 29) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACLMMOD0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACLMMOD0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(164)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(287 29) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACMDLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACMDLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(35)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(38 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACSSLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACSSLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(226)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(90 9) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACUNHF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACUNHF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(11 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACUNJCL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACUNJCL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACUNLOCL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACUNLOCL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(45)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(972 98) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACUNMAC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACUNMAC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(91 10) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACUNMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACUNMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(259)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACUNMOD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACUNMOD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(49)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(83 9) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACUNSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACUNSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(45 5) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ACUNTBL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ACUNTBL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(256)           -
       DSORG(PO)           -
       DIR(1027)             -
       BLKSIZE(0)              -
       AVBLOCK(27904)         -
       TRACKS SPACE(41697 4168) -
       UNIT(3390)            -
       VOLUME(ZOS244)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ADBBLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ADBBLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(35)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(54 6) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ADFMAC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ADFMAC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ADFPMOD0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ADFPMOD0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(17)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(29 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ADFQMLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ADFQMLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ADFQPLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ADFQPLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(17)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(37 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ADFQPSRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ADFQPSRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(15 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ADGTCLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ADGTCLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(15)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(178 18) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ADGTLLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ADGTLLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(255)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(251 25) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ADGTMLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ADGTMLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(34)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(51 5) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ADGTPLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ADGTPLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(407)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(880 88) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ADGTPSRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ADGTPSRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(77)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(230 23) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ADGTSLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ADGTSLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(36 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ADGTTLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ADGTTLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ADGTTSRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ADGTTSRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ADMSSVM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ADMSSVM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(152)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(40 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AEDGEXE1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AEDGEXE1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(110 11) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AEDGHFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AEDGHFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(256)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(98 10) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AEDGMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AEDGMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(11)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AEDGMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AEDGMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(28)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(60 6) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AEDGPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AEDGPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(91)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(189 19) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AEDGPSRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AEDGPSRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(33)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(66 7) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AEDGSRC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AEDGSRC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(21 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AEEQINST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AEEQINST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3212 322) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AERBCLS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AERBCLS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(31 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AERBMAC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AERBMAC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(69 7) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AERBMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AERBMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AERBMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AERBMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(141)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(552 55) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AERBPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AERBPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(140)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(375 38) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AERBPWSV)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AERBPWSV')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5902 591) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AERBT)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AERBT')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(22 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AERBTENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AERBTENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AFNTILIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AFNTILIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B M ) -
       LRECL(16124)           -
       DSORG(PO)           -
       DIR(473)             -
       BLKSIZE(16128)              -
       AVBLOCK(16128)         -
       TRACKS SPACE(561 56) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AFNTLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AFNTLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B M ) -
       LRECL(12284)           -
       DSORG(PO)           -
       DIR(172)             -
       BLKSIZE(12888)              -
       AVBLOCK(12888)         -
       TRACKS SPACE(275 28) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AFNTLIBB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AFNTLIBB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B M ) -
       LRECL(12284)           -
       DSORG(PO)           -
       DIR(545)             -
       BLKSIZE(12288)              -
       AVBLOCK(12288)         -
       TRACKS SPACE(3266 326) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AFNT300)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AFNT300')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B M ) -
       LRECL(12284)           -
       DSORG(PO)           -
       DIR(509)             -
       BLKSIZE(12288)              -
       AVBLOCK(12288)         -
       TRACKS SPACE(3587 358) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AFOMHDRS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AFOMHDRS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(25 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AFOMHFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AFOMHFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(153)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(27486 2749) -
       UNIT(3390)            -
       VOLUME(ZOS244)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AFOMMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AFOMMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(38)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(586 59) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AFOMOBJ)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AFOMOBJ')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(39)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(72 8) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AFONTHFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AFONTHFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(27904)         -
       TRACKS SPACE(263 27) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AGDEMOD0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AGDEMOD0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(47)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(67 7) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AGENLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AGENLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AGFTAJL1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AGFTAJL1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AGFTAMD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AGFTAMD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(20)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(24 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AGFUMOD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AGFUMOD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(21)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(37 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AHASMAC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AHASMAC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(59)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(558 56) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AHASMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AHASMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(259)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(2590)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AHASMOD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AHASMOD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(69)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(170 17) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AHASPARM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AHASPARM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AHASPNL0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AHASPNL0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(21 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AHASSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AHASSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(102 11) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AHASSRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AHASSRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(26)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(2453 246) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AHELP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AHELP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(46)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(236 24) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AHELPENP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AHELPENP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(36 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AICELIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AICELIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(81)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(91 9) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AICESAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AICESAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AICESRCE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AICESRCE')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AICEUSER)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AICEUSER')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(19 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AICQAB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AICQAB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AICQILIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AICQILIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AICQMAC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AICQMAC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(223)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(343 34) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AICQMAC2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AICQMAC2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AICQMAC3)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AICQMAC3')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(16)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(19 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AICQMAC4)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AICQMAC4')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AICQMAC5)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AICQMAC5')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(169 17) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AIEAHDR)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AIEAHDR')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(14)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(241 25) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AIEAHDRV)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AIEAHDRV')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(260)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(28 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AIEALNKE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AIEALNKE')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3462 347) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AIEAMIGE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AIEAMIGE')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(16 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AIEASID)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AIEASID')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AIEWMOD0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AIEWMOD0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(53)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(93 10) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AIFALIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AIFALIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(12 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AIGDVBS1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AIGDVBS1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(4100)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(23 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AIKYHFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AIKYHFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(1653 166) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AIMAGE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AIMAGE')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(17)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(639 64) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AIMFMOD0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AIMFMOD0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(11 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AINDLMD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AINDLMD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AISTASGD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AISTASGD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(512)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8192)         -
       TRACKS SPACE(63 7) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AISTASN1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AISTASN1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(1024)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8192)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AISTCMIP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AISTCMIP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AISTDAT1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AISTDAT1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(6156)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(29 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AISTDAT2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AISTDAT2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(259)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AISTGDMO)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AISTGDMO')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(500)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8000)         -
       TRACKS SPACE(134 14) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AISTMAC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AISTMAC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(19 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AITYHFS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AITYHFS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(103 11) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ALINKLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ALINKLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(181)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(194 19) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ALPALIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ALPALIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(32)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(29 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AMACLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AMACLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(141)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(4139 414) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AMIGLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AMIGLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(194)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(166 17) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AMODGEN)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AMODGEN')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(59)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(718 72) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AMSGENP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AMSGENP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(259)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(1040)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AMSGENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AMSGENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(259)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(1040)         -
       TRACKS SPACE(81 9) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ANFSLIBE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ANFSLIBE')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(308 31) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ANFSMAC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ANFSMAC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ANFSSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ANFSSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(16 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ANFSTARB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ANFSTARB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(264)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(42 5) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ANUCLEUS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ANUCLEUS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(47)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(41 4) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOSACB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOSACB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(85)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(112 11) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOSA0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOSA0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(104)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(161 16) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOSA1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOSA1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(71)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(120 12) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOSBN)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOSBN')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(260)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(391 39) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOSB0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOSB0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOSB3)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOSB3')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(166)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(189 19) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOSCA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOSCA')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(7 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOSCD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOSCD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(173)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(172 17) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOSCE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOSCE')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOSC2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOSC2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOSC5)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOSC5')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(1187)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(1867 185) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOSC6)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOSC6')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOSD0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOSD0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(148)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(148 15) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOSD7)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOSD7')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOSG0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOSG0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOSJSC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOSJSC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOST3)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOST3')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(20)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(17 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOST4)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOST4')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(142)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(143 14) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOSU0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOSU0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(237)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(418 42) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOSXCF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOSXCF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(137)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(256 26) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOS00)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOS00')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(17)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(30 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOS04)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOS04')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOS05)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOS05')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOS06)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOS06')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(15)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOS11)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOS11')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(11)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOS12)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOS12')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(123)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(227 23) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOS24)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOS24')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(11 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOS26)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOS26')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(857)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(935 92) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOS28)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOS28')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(226)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(317 32) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AOS32)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AOS32')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(89)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(69 7) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ASAMPLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ASAMPLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(85)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(980 98) -
       UNIT(3390)            -
       VOLUME(ZOS244)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ASSFMOD0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ASSFMOD0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(51)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(128 13) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.ATSOMAC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.ATSOMAC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(13)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(81 8) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.AUADS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.AUADS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.CBRDBRM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.CBRDBRM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(12 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.CIPLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.CIPLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.CMDLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.CMDLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(64)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(83 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.CSSLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.CSSLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(236)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(103 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.DBBLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.DBBLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(35)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(53 6) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.DFQLLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.DFQLLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(9 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.DFQMLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.DFQMLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.DFQPLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.DFQPLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(17)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(37 4) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.DGTCLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.DGTCLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(15)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(178 18) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.DGTLLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.DGTLLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(128)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(243 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.DGTMLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.DGTMLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(34)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(51 5) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.DGTPLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.DGTPLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(407)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(880 88) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.DGTSLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.DGTSLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(36 4) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.DGTTLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.DGTTLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.FONTLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.FONTLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B M ) -
       LRECL(12284)           -
       DSORG(PO)           -
       DIR(172)             -
       BLKSIZE(12888)              -
       AVBLOCK(12888)         -
       TRACKS SPACE(275 28) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.FONTLIBB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.FONTLIBB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B M ) -
       LRECL(12284)           -
       DSORG(PO)           -
       DIR(545)             -
       BLKSIZE(12288)              -
       AVBLOCK(12288)         -
       TRACKS SPACE(3266 326) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.FONT300)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.FONT300')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B M ) -
       LRECL(12284)           -
       DSORG(PO)           -
       DIR(509)             -
       BLKSIZE(12288)              -
       AVBLOCK(12288)         -
       TRACKS SPACE(3587 358) -
       UNIT(3390)            -
       VOLUME(ZOS242)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.HELP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.HELP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(46)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(236 24) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.HELPENP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.HELPENP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(36 4) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.HHRFCLST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.HHRFCLST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(22 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.HHRFMSG)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.HHRFMSG')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.HHRFPANL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.HHRFPANL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(94)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(229 23) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.HHRFSKEL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.HHRFSKEL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(15)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(69 7) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.HRFCLST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.HRFCLST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(22 3) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.HRFMSG)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.HRFMSG')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.HRFPANL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.HRFPANL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(94)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(229 23) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.HRFSKEL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.HRFSKEL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(15)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(69 7) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.IBM.APARMLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.IBM.APARMLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(23 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.IBM.APROCLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.IBM.APROCLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.IBM.PARMLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.IBM.PARMLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(25 3) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.IBM.PROCLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.IBM.PROCLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(11 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.IGDVBS1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.IGDVBS1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(4100)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.IMAGELIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.IMAGELIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(48)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(42 5) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.IPLPARM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.IPLPARM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(80)         -
       TRACKS SPACE(25 3) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.LINKLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.LINKLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(868)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3979 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.LPALIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.LPALIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(392)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(1185 118) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.MACLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.MACLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(157)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(4752 475) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.MIGLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.MIGLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(427)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(1261 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.MODGEN)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.MODGEN')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(59)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(719 72) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.MSGENP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.MSGENP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(259)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(1040)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.MSGENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.MSGENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(259)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(1040)         -
       TRACKS SPACE(78 8) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.NFSLIBE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.NFSLIBE')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(357 36) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.NFSMAC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.NFSMAC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.NFSSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.NFSSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(16 2) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.NFSTARB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.NFSTARB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(264)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(27 3) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.PARMLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.PARMLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(7 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SACBCNTL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SACBCNTL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(32 4) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SADRYLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SADRYLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SAMPLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SAMPLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(104)             -
       BLKSIZE(0)              -
       AVBLOCK(3120)         -
       TRACKS SPACE(1871 187) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SAXREXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SAXREXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(21 3) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SBDTCLI0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SBDTCLI0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SBDTCMD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SBDTCMD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(4 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SBDTHELP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SBDTHELP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SBDTLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SBDTLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(14)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(24 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SBDTLINK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SBDTLINK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(4 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SBDTLPA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SBDTLPA')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SBDTMAC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SBDTMAC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(20)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(129 13) -
       UNIT(3390)            -
       VOLUME(ZOS242)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SBDTMSG)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SBDTMSG')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SBDTPNL0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SBDTPNL0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SBDTSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SBDTSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SBLSCLI0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SBLSCLI0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(20)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(355 36) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SBLSKEL0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SBLSKEL0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(12 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SBLSMSG0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SBLSMSG0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(13 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SBLSPNL0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SBLSPNL0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(245)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(414 41) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SBLSTBL0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SBLSTBL0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SBPXEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SBPXEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(46 5) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SBPXMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SBPXMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(13 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SBPXPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SBPXPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(28)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(37 4) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SBPXTENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SBPXTENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SCBDCLST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SCBDCLST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SCBDHENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SCBDHENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(553)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(516 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SCBDMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SCBDMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(82)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(30 3) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SCBDPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SCBDPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(119)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(92 9) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SCBDTEMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SCBDTEMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(23 3) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SCBDTENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SCBDTENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SCCR3BND)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SCCR3BND')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(31)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(15 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SCCR6BND)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SCCR6BND')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(31)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(16 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SCDRTABL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SCDRTABL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(289 29) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SCUNHF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SCUNHF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(11 2) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SCUNJCL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SCUNJCL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SCUNLOCL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SCUNLOCL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(45)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(972 98) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SCUNMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SCUNMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(259)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SCUNTBL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SCUNTBL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(256)           -
       DSORG(PO)           -
       DIR(1027)             -
       BLKSIZE(0)              -
       AVBLOCK(27904)         -
       TRACKS SPACE(41697 4168) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SDFQPSRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SDFQPSRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(15 2) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SDGTPSRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SDGTPSRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(77)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(230 23) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SDGTTSRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SDGTTSRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SDMSSVM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SDMSSVM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(152)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(40 4) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SDMSSVMS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SDMSSVMS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SDWWDLPA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SDWWDLPA')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SEDGEXE1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SEDGEXE1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(110 11) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SEDGMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SEDGMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(11)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SEDGPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SEDGPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(91)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(189 19) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SEDGPSRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SEDGPSRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(33)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(66 7) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SEEQINST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SEEQINST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3212 322) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SERBCLS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SERBCLS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(32 4) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SERBLINK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SERBLINK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(44)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(651 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SERBLPA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SERBLPA')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(13)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SERBMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SERBMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SERBPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SERBPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(140)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(373 37) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SERBPWSV)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SERBPWSV')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5864 587) -
       UNIT(3390)            -
       VOLUME(ZOS242)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SERBT)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SERBT')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(21 3) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SERBTENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SERBTENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SFNTILIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SFNTILIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B M ) -
       LRECL(16124)           -
       DSORG(PO)           -
       DIR(473)             -
       BLKSIZE(16128)              -
       AVBLOCK(16128)         -
       TRACKS SPACE(561 56) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SFOMHDRS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SFOMHDRS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(20 2) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SFOMOBJ)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SFOMOBJ')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(39)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(72 8) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SHASLNKE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SHASLNKE')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(122 13) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SHASMAC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SHASMAC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(59)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(558 56) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SHASMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SHASMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(259)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(2590)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SHASMIG)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SHASMIG')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(146)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(147 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SHASPARM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SHASPARM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SHASPNL0)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SHASPNL0')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(21 3) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SHASSAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SHASSAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(102 11) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SHASSRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SHASSRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(26)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(2453 246) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SICELINK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SICELINK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(11)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(30 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SICELPA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SICELPA')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SICEPROC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SICEPROC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SICESAMP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SICESAMP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(9 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SICESRCE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SICESRCE')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SICEUSER)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SICEUSER')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(19 2) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SIEAHDR.H)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SIEAHDR.H')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(14)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(242 25) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SIEAHDRV.H)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SIEAHDRV.H')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(260)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(28 3) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SIEALNKE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SIEALNKE')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3588 359) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SIEAMIGE)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SIEAMIGE')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(18 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SIEASID)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SIEASID')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SIFALIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SIFALIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SISTASGD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SISTASGD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(512)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8192)         -
       TRACKS SPACE(63 7) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SISTASN1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SISTASN1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(1024)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8192)         -
       TRACKS SPACE(14 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SISTCLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SISTCLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(112)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(132 13) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SISTCMIP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SISTCMIP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SISTDAT1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SISTDAT1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(6156)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(29 3) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SISTDAT2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SISTDAT2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(259)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6475)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SISTGDMO)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SISTGDMO')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(500)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8000)         -
       TRACKS SPACE(134 14) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SISTMAC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SISTMAC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(19 2) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SORTLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SORTLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(35)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(32 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SORTLPA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SORTLPA')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(13)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(35 4) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.SVCLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.SVCLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(4 0) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.UADS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.UADS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(800)              -
       AVBLOCK(800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.VTAMLIB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.VTAMLIB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(71)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(309 31) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.SYS1.VTAMLST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.SYS1.VTAMLST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(6160)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.AEZADBR1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.AEZADBR1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.AEZAMAC1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.AEZAMAC1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(14)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(108 11) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.AEZAMAC2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.AEZAMAC2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(92)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(402 40) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.AEZAMAC3)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.AEZAMAC3')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(27)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(144 15) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.AEZAMODS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.AEZAMODS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(3347 335) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.AEZARNT1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.AEZARNT1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(67)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(119 12) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.AEZARNT2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.AEZARNT2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(22 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.AEZARNT3)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.AEZARNT3')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(13)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(82 9) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.AEZARNT4)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.AEZARNT4')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(12 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.AEZAROE1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.AEZAROE1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(63)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(113 12) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.AEZAROE2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.AEZAROE2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(22 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.AEZAROE3)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.AEZAROE3')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(13)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(82 9) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.AEZASMP1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.AEZASMP1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(112)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(1637 164) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.AEZAXLTD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.AEZAXLTD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F   ) -
       LRECL(256)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(256)              -
       AVBLOCK(256)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.AEZAXLTK)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.AEZAXLTK')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(5124)           -
       DSORG(PO)           -
       DIR(13)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(103 11) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.AEZAXLT1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.AEZAXLT1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.AEZAXLT2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.AEZAXLT2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.AEZAXLT3)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.AEZAXLT3')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(231)           -
       DSORG(PO)           -
       DIR(86)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(1335 134) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZACMAC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZACMAC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(94)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(321 32) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZACMTX)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZACMTX')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(166)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(41 4) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZADBCX)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZADBCX')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(231)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(230 23) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZADBRM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZADBRM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZADPIL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZADPIL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZADSIL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZADSIL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(27 3) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZADSIM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZADSIM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZADSIP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZADSIP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZAEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZAEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZAINST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZAINST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(55)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(213 22) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZALIBN)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZALIBN')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(38)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZALNK2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZALNK2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(15 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZALOAD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZALOAD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(2742 275) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZALPA)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZALPA')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZAMENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZAMENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZANCLS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZANCLS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(13)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(44 5) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZANMAC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZANMAC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(93 10) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZANPNL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZANPNL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(21)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(24 3) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZAOLDX)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZAOLDX')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZAPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZAPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(16 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZARNT1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZARNT1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(67)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(119 12) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZARNT2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZARNT2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(22 3) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZARNT3)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZARNT3')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(13)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(82 9) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZARNT4)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZARNT4')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(12 2) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZAROE1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZAROE1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(63)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(113 12) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZAROE2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZAROE2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(22 3) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZAROE3)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZAROE3')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(13)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(82 9) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZARPCL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZARPCL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(34)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(11 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZATCP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZATCP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(99 10) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZATCPX)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZATCPX')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZATELX)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZATELX')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZAXAWL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZAXAWL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(44)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(16 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZAXLD1)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZAXLD1')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F   ) -
       LRECL(256)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(256)              -
       AVBLOCK(256)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZAXLD2)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZAXLD2')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(5124)           -
       DSORG(PO)           -
       DIR(11)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(89 9) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZAXMLB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZAXMLB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(155)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(57 6) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZAXTLB)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZAXTLB')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(98)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(22 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24MST.TCPIP.SEZAX11L)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24MST.TCPIP.SEZAX11L')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(245)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(62 6) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TCPIVP.TCPPARMS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TCPIVP.TCPPARMS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8800)         -
       TRACKS SPACE(51 6) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANCMD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANCMD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(22 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANCUS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANCUS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(939)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(1543 153) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANDATV)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANDATV')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(6160)           -
       DSORG(PO)           -
       DIR(23)             -
       BLKSIZE(0)              -
       AVBLOCK(8900)         -
       TRACKS SPACE(409 41) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(46)             -
       BLKSIZE(0)              -
       AVBLOCK(8900)         -
       TRACKS SPACE(54 6) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANHENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANHENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(175)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(262 26) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANISP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANISP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANJAR)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANJAR')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(27998)         -
       TRACKS SPACE(379 38) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANMAC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANMAC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(23 3) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANMOD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANMOD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(292)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(580 58) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANMODL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANMODL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(203)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(2489 249) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANMODP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANMODP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(114 12) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANMODR)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANMODR')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANMODS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANMODS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(123)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(218 22) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANOSRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANOSRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8900)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANPAR)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANPAR')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(13)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(100 10) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(676)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(1293 128) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANPKGI)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANPKGI')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(15)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(141 15) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANSAM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANSAM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(67)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(739 74) -
       UNIT(3390)            -
       VOLUME(ZOS245)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANSQL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANSQL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANUTIN)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANUTIN')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKANWENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKANWENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(158)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(195 20) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKCIINST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKCIINST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(125 13) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKNSLOCL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKNSLOCL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(6160)           -
       DSORG(PO)           -
       DIR(45)             -
       BLKSIZE(0)              -
       AVBLOCK(8900)         -
       TRACKS SPACE(70 7) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKOBDATF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKOBDATF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKOBHELP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKOBHELP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(79)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(21 2) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKOCHELP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKOCHELP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(26)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.DKOCPROC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.DKOCPROC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(525)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(123 12) -
       UNIT(3390)            -
       VOLUME(ZOS246)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANCMD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANCMD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(22 3) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANCUS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANCUS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(939)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(1543 153) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANDATV)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANDATV')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(6160)           -
       DSORG(PO)           -
       DIR(23)             -
       BLKSIZE(0)              -
       AVBLOCK(8900)         -
       TRACKS SPACE(409 41) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANEXEC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANEXEC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(46)             -
       BLKSIZE(0)              -
       AVBLOCK(8900)         -
       TRACKS SPACE(54 6) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANHENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANHENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(175)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(262 26) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANISP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANISP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANMAC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANMAC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(23 3) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANMOD)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANMOD')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(86)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(746 75) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANMODL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANMODL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(202)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(2473 247) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANMODP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANMODP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(LIBRARY)            -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(383 39) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANMODR)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANMODR')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(4 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANMODS)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANMODS')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(U   ) -
       LRECL(0)           -
       DSORG(PO)           -
       DIR(185)             -
       BLKSIZE(32760)              -
       AVBLOCK(6144)         -
       TRACKS SPACE(232 23) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANOSRC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANOSRC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(255)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8900)         -
       TRACKS SPACE(8 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANPAR)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANPAR')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(13)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(100 10) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANPENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANPENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(676)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(1293 128) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANPKGI)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANPKGI')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(15)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(141 15) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANSAM)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANSAM')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(67)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(739 74) -
       UNIT(3390)            -
       VOLUME(ZOS243)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANSQL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANSQL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(6 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANUTIN)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANUTIN')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(3 1) -
       UNIT(3390)            -
       VOLUME(ZOS247)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKANWENU)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKANWENU')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(158)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(195 20) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKCIINST)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKCIINST')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(100)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(125 13) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKNSLOCL)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKNSLOCL')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(V B  ) -
       LRECL(6160)           -
       DSORG(PO)           -
       DIR(45)             -
       BLKSIZE(0)              -
       AVBLOCK(8900)         -
       TRACKS SPACE(70 7) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKOBDATF)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKOBDATF')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(10)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(5 1) -
       UNIT(3390)            -
       VOLUME(ZOS241)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKOBHELP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKOBHELP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(79)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(21 2) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKOCHELP)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKOCHELP')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(26)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(10 1) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ALLOC1),(4000,LT))
//*
//*************************************************************
//*   THE FOLLOWING STEP MAKES SURE THAT THE *ONLY* POSSIBLE REASON
//*   FOR A NONZERO RETURN CODE FROM LISTCAT IS "DATA SET NOT FOUND".
//*************************************************************
//*
//CHKDSN4 EXEC PGM=IKJEFT1B,COND=(4000,LT)
//SYSEXEC  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPCENU
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQEXEC
//         DD DISP=SHR,DSN=AOP.SAOPEXEC
//         DD DISP=SHR,DSN=EUVF.SEUVFEXC
//         DD DISP=SHR,DSN=SYS1.SEDGEXE1
//         DD DISP=SHR,DSN=EOY.SEOYCLIB
//         DD DISP=SHR,DSN=EOX.SEPHCLB1
//         DD DISP=SHR,DSN=ISP.SISPEXEC
//         DD DISP=SHR,DSN=IOE.SIOEEXEC
//         DD DISP=SHR,DSN=SYS1.SBPXEXEC
//         DD DISP=SHR,DSN=ISF.SISFEXEC
//         DD DISP=SHR,DSN=GLD.SGLDEXEC
//         DD DISP=SHR,DSN=FFST.V120ESA.SEPWSRC1
//SYSPROC  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPCENU
//         DD DISP=SHR,DSN=CPAC.CMDPROC
//         DD DISP=SHR,DSN=SYS1.SBLSCLI0
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJCLST
//         DD DISP=SHR,DSN=CBC.SCCNUTL
//         DD DISP=SHR,DSN=SYS1.DGTCLIB
//         DD DISP=SHR,DSN=CSF.SCSFCLI0
//         DD DISP=SHR,DSN=EOY.SEOYCLIB
//         DD DISP=SHR,DSN=EOX.SEPHCLB1
//         DD DISP=SHR,DSN=SYS1.SERBCLS
//         DD DISP=SHR,DSN=ISP.SISPCLIB
//         DD DISP=SHR,DSN=SYS1.HRFCLST
//         DD DISP=SHR,DSN=SYS1.SBDTCLI0
//         DD DISP=SHR,DSN=GIM.SGIMCLS0
//         DD DISP=SHR,DSN=ICQ.ICQCCLIB
//         DD DISP=SHR,DSN=SYS1.SCBDCLST
//         DD DISP=SHR,DSN=GDDM.SADMSAM
//         DD DISP=SHR,DSN=FFST.V120ESA.SEPWCENU
//ISPMLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPMENU
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJMLIB
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQMSGE
//         DD DISP=SHR,DSN=TCPIP.SEZAMENU
//         DD DISP=SHR,DSN=AOP.SAOPMENU
//         DD DISP=SHR,DSN=SYS1.DGTMLIB
//         DD DISP=SHR,DSN=SYS1.DFQMLIB
//         DD DISP=SHR,DSN=SYS1.SEDGMENU
//         DD DISP=SHR,DSN=CSF.SCSFMSG0
//         DD DISP=SHR,DSN=EOY.SEOYBENU
//         DD DISP=SHR,DSN=EOY.SEOYMENU
//         DD DISP=SHR,DSN=EOX.SEPHMSG1
//         DD DISP=SHR,DSN=SYS1.SERBMENU
//         DD DISP=SHR,DSN=ISP.SISPMENU
//         DD DISP=SHR,DSN=SYS1.HRFMSG
//         DD DISP=SHR,DSN=SYS1.SBDTMSG
//         DD DISP=SHR,DSN=SYS1.SBLSMSG0
//         DD DISP=SHR,DSN=SYS1.SBPXMENU
//         DD DISP=SHR,DSN=ISF.SISFMLIB
//         DD DISP=SHR,DSN=GIM.SGIMMENU
//         DD DISP=SHR,DSN=ICQ.ICQMLIB
//         DD DISP=SHR,DSN=SYS1.SCBDMENU
//         DD DISP=SHR,DSN=GDDM.SADMMSG
//ISPPLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPPENU
//         DD DISP=SHR,DSN=CPAC.ISPPLIB
//         DD DISP=SHR,DSN=ISP.SISPPENU
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJPLIB
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQPNLE
//         DD DISP=SHR,DSN=TCPIP.SEZAPENU
//         DD DISP=SHR,DSN=SYS1.SBLSPNL0
//         DD DISP=SHR,DSN=AOP.SAOPPENU
//         DD DISP=SHR,DSN=SYS1.DFQPLIB
//         DD DISP=SHR,DSN=SYS1.DGTPLIB
//         DD DISP=SHR,DSN=SYS1.SEDGPENU
//         DD DISP=SHR,DSN=CSF.SCSFPNL0
//         DD DISP=SHR,DSN=EOY.SEOYPENU
//         DD DISP=SHR,DSN=EOX.SEPHPNL1
//         DD DISP=SHR,DSN=SYS1.SERBPENU
//         DD DISP=SHR,DSN=SYS1.HRFPANL
//         DD DISP=SHR,DSN=SYS1.SBDTPNL0
//         DD DISP=SHR,DSN=SYS1.SHASPNL0
//         DD DISP=SHR,DSN=SYS1.SBPXPENU
//         DD DISP=SHR,DSN=ISF.SISFPLIB
//         DD DISP=SHR,DSN=GIM.SGIMPENU
//         DD DISP=SHR,DSN=ICQ.ICQPLIB
//         DD DISP=SHR,DSN=SYS1.SCBDPENU
//         DD DISP=SHR,DSN=GDDM.SADMPNL
//         DD DISP=SHR,DSN=FFST.V120ESA.SEPWPENU
//ISPSLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPSENU
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJSLIB
//         DD DISP=SHR,DSN=SYS1.DGTSLIB
//         DD DISP=SHR,DSN=CSF.SCSFSKL0
//         DD DISP=SHR,DSN=ISP.SISPSLIB
//         DD DISP=SHR,DSN=ISP.SISPSENU
//         DD DISP=SHR,DSN=SYS1.HRFSKEL
//         DD DISP=SHR,DSN=SYS1.SBLSKEL0
//         DD DISP=SHR,DSN=ISF.SISFSLIB
//         DD DISP=SHR,DSN=GIM.SGIMSENU
//         DD DISP=SHR,DSN=ICQ.ICQSLIB
//ISPLLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPLOAD
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJMOD1
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQANLE
//         DD DISP=SHR,DSN=SYS1.DFQLLIB
//         DD DISP=SHR,DSN=SYS1.DGTLLIB
//ISPTLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPTENU
//         DD DISP=SHR,DSN=E002884.ISPF.ISPPROF
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJTLIB
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQTBLE
//         DD DISP=SHR,DSN=SYS1.DGTTLIB
//         DD DISP=SHR,DSN=CSF.SCSFTLIB
//         DD DISP=SHR,DSN=EOY.SEOYTENU
//         DD DISP=SHR,DSN=EOX.SEPHTBL1
//         DD DISP=SHR,DSN=SYS1.SERBT
//         DD DISP=SHR,DSN=SYS1.SERBTENU
//         DD DISP=SHR,DSN=ISP.SISPTENU
//         DD DISP=SHR,DSN=SYS1.SBLSTBL0
//         DD DISP=SHR,DSN=SYS1.SBPXTENU
//         DD DISP=SHR,DSN=ISF.SISFTLIB
//         DD DISP=SHR,DSN=GIM.SGIMTENU
//         DD DISP=SHR,DSN=ICQ.ICQTABLS
//         DD DISP=SHR,DSN=SYS1.SCBDTENU
//ISPLOG   DD DSN=&ISPLOG,
//            DISP=NEW,UNIT=SYSALLDA,SPACE=(CYL,(1,1,1)),
//            DCB=(RECFM=FB,BLKSIZE=3120,LRECL=80)
//ISPPROF  DD DSN=&ISPPROF,
//            DISP=NEW,UNIT=SYSALLDA,SPACE=(CYL,(1,1,1)),
//            DCB=(RECFM=FB,BLKSIZE=3120,LRECL=80)
//SYSPRINT DD SYSOUT=*
//SYSTSPRT DD SYSOUT=*
//SYSTSIN  DD *
 PROFILE PREFIX(E002884)
 ISPSTART CMD(%CPPEDSN CHECKRC F )
/*
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,CHKDSN4),(4000,LT))
//*
//*
//*************************************************************
//*
//*  THIS IS TO CHECK WHETHER SMS IS ACTIVE IN THE DRIVING
//*  SYSTEM. IT USES THE REXX MVSVAR FUNCTION TO CHECK IF SMS
//*  IS ACTIVE AND RETURN THE CORRESPONDING RETURN CODE.
//*
//*  IF SMS
//*       ACTIVE   ---    RETURN 0
//*     INACTIVE   ---    RETURN 4
//*  UNAVAILABLE   ---    RETURN 8
//*    ALL OTHER   ---    RETURN 12
//*
//*************************************************************
//CHKSMS6 EXEC PGM=IKJEFT1B
//SYSEXEC  DD DSN=ZOS24.OS241147.ORDER.SCPPCENU,DISP=SHR
//SYSTSPRT DD SYSOUT=*
//SYSTSIN  DD *
  %CPPESMS NOISPF
/*
//*
//NOTOK EXEC  PGM=CPPMAXRC,COND=((0,GE,CHKSMS6),(4000,LT))
//*
//ALLOC2 EXEC PGM=IDCAMS,DYNAMNBR=1010,COND=(4000,LT)
//SYSPRINT DD SYSOUT=*
//SYSIN    DD *

    LISTCAT -
      ENTRY(Z24SUP.TIVOLI.TKOCPROC)

  IF LASTCC GT 0 THEN DO
    ALLOCATE -
       DSNAME('Z24SUP.TIVOLI.TKOCPROC')          -
       NEW                         -
       CATALOG                     -
       DSNTYPE(PDS)                -
       RECFM(F B  ) -
       LRECL(80)           -
       DSORG(PO)           -
       DIR(525)             -
       BLKSIZE(0)              -
       AVBLOCK(8880)         -
       TRACKS SPACE(123 12) -
       UNIT(3390)            -
       VOLUME(ZOS240)

  END

  IF LASTCC = 0 THEN SET MAXCC = 0
  ELSE CANCEL
//*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ALLOC2),(4000,LT))
//*
//*
//*************************************************************
//*
//*  THIS IS TO CHECK WHETHER SMS IS ACTIVE IN THE DRIVING
//*  SYSTEM. IT USES THE REXX MVSVAR FUNCTION TO CHECK IF SMS
//*  IS ACTIVE AND RETURN THE CORRESPONDING RETURN CODE.
//*
//*  IF SMS
//*       ACTIVE   ---    RETURN 0
//*     INACTIVE   ---    RETURN 4
//*  UNAVAILABLE   ---    RETURN 8
//*    ALL OTHER   ---    RETURN 12
//*
//*************************************************************
//CHKSMS7 EXEC PGM=IKJEFT1B
//SYSEXEC  DD DSN=ZOS24.OS241147.ORDER.SCPPCENU,DISP=SHR
//SYSTSPRT DD SYSOUT=*
//SYSTSIN  DD *
  %CPPESMS NOISPF
/*
//*
//NOTOK EXEC  PGM=CPPMAXRC,COND=((0,GE,CHKSMS7),(4000,LT))
//*
//*--------------------------------------------------------------------
//* DOC: STEP ALCCDS ALLOCATES THE NEW SYSPLEX COUPLE DATASETS
//*--------------------------------------------------------------------
//ALCCDS   EXEC PGM=IXCL1DSU,COND=(4000,LT)
//STEPLIB   DD DSN=SYS1.MIGLIB,DISP=SHR
//SYSPRINT DD SYSOUT=*
//SYSIN     DD *
  /* -------------------------------------------------------------- */
  /* ALLOCATE SYSTEM LOGGER COUPLE DATASET LOGCDS01                 */
  /* -------------------------------------------------------------- */
  DEFINEDS SYSPLEX(LOCAL)
       DSN(Z24MST.SYS1.LOGR.CDS01)
       VOLSER(ZOS247)
       MAXSYSTEM(8)
       CATALOG
    DATA TYPE(LOGR)
         ITEM NAME(LSR) NUMBER(10)
         ITEM NAME(LSTRR) NUMBER(10)
         ITEM NAME(DSEXTENT) NUMBER(10)
  /* -------------------------------------------------------------- */
  /* ALLOCATE SYSTEM LOGGER COUPLE DATASET LOGCDS02                 */
  /* -------------------------------------------------------------- */
  DEFINEDS SYSPLEX(LOCAL)
       DSN(Z24MST.SYS1.LOGR.CDS02)
       VOLSER(ZOS247)
       MAXSYSTEM(8)
       CATALOG
    DATA TYPE(LOGR)
         ITEM NAME(LSR) NUMBER(10)
         ITEM NAME(LSTRR) NUMBER(10)
         ITEM NAME(DSEXTENT) NUMBER(10)
  /* -------------------------------------------------------------- */
  /* ALLOCATE SYSTEM LOGGER COUPLE DATASET LOGCDS03                 */
  /* -------------------------------------------------------------- */
  DEFINEDS SYSPLEX(LOCAL)
       DSN(Z24MST.SYS1.LOGR.CDS03)
       VOLSER(ZOS247)
       MAXSYSTEM(8)
       CATALOG
    DATA TYPE(LOGR)
         ITEM NAME(LSR) NUMBER(10)
         ITEM NAME(LSTRR) NUMBER(10)
         ITEM NAME(DSEXTENT) NUMBER(10)
  /* -------------------------------------------------------------- */
  /* ALLOCATE WLM COUPLE DATASET WLMCDS1                            */
  /* -------------------------------------------------------------- */
  DEFINEDS SYSPLEX(LOCAL)
       DSN(Z24MST.SYS1.WLM.CDS01)
       VOLSER(ZOS247)
       MAXSYSTEM(8)
       CATALOG
    DATA TYPE(WLM)
         ITEM NAME(APPLENV) NUMBER(100)
         ITEM NAME(POLICY) NUMBER(10)
         ITEM NAME(SCHENV) NUMBER(100)
         ITEM NAME(SRVCLASS) NUMBER(30)
         ITEM NAME(SVAEAEXT) NUMBER(5)
         ITEM NAME(SVDCREXT) NUMBER(5)
         ITEM NAME(SVDEFEXT) NUMBER(5)
         ITEM NAME(SVSEAEXT) NUMBER(5)
         ITEM NAME(WORKLOAD) NUMBER(35)
  /* -------------------------------------------------------------- */
  /* ALLOCATE WLM COUPLE DATASET WLMCDS2                            */
  /* -------------------------------------------------------------- */
  DEFINEDS SYSPLEX(LOCAL)
       DSN(Z24MST.SYS1.WLM.CDS02)
       VOLSER(ZOS247)
       MAXSYSTEM(8)
       CATALOG
    DATA TYPE(WLM)
         ITEM NAME(APPLENV) NUMBER(100)
         ITEM NAME(POLICY) NUMBER(10)
         ITEM NAME(SCHENV) NUMBER(100)
         ITEM NAME(SRVCLASS) NUMBER(30)
         ITEM NAME(SVAEAEXT) NUMBER(5)
         ITEM NAME(SVDCREXT) NUMBER(5)
         ITEM NAME(SVDEFEXT) NUMBER(5)
         ITEM NAME(SVSEAEXT) NUMBER(5)
         ITEM NAME(WORKLOAD) NUMBER(35)
  /* -------------------------------------------------------------- */
  /* ALLOCATE WLM COUPLE DATASET WLMCDS3                            */
  /* -------------------------------------------------------------- */
  DEFINEDS SYSPLEX(LOCAL)
       DSN(Z24MST.SYS1.WLM.CDS03)
       VOLSER(ZOS247)
       MAXSYSTEM(8)
       CATALOG
    DATA TYPE(WLM)
         ITEM NAME(APPLENV) NUMBER(100)
         ITEM NAME(POLICY) NUMBER(10)
         ITEM NAME(SCHENV) NUMBER(100)
         ITEM NAME(SRVCLASS) NUMBER(30)
         ITEM NAME(SVAEAEXT) NUMBER(5)
         ITEM NAME(SVDCREXT) NUMBER(5)
         ITEM NAME(SVDEFEXT) NUMBER(5)
         ITEM NAME(SVSEAEXT) NUMBER(5)
         ITEM NAME(WORKLOAD) NUMBER(35)
  /* -------------------------------------------------------------- */
  /* ALLOCATE SYSPLEX COUPLE DATASET XCFCDS01                       */
  /* -------------------------------------------------------------- */
  DEFINEDS SYSPLEX(LOCAL)
       DSN(Z24MST.SYS1.XCF.CDS01)
       VOLSER(ZOS247)
       MAXSYSTEM(8)
       CATALOG
    DATA TYPE(SYSPLEX)
       ITEM NAME(GROUP)    NUMBER(50)
       ITEM NAME(MEMBER)   NUMBER(100)
       ITEM NAME(GRS)      NUMBER(1)
  /* -------------------------------------------------------------- */
  /* ALLOCATE SYSPLEX COUPLE DATASET XCFCDS02                       */
  /* -------------------------------------------------------------- */
  DEFINEDS SYSPLEX(LOCAL)
       DSN(Z24MST.SYS1.XCF.CDS02)
       VOLSER(ZOS247)
       MAXSYSTEM(8)
       CATALOG
    DATA TYPE(SYSPLEX)
       ITEM NAME(GROUP)    NUMBER(50)
       ITEM NAME(MEMBER)   NUMBER(100)
       ITEM NAME(GRS)      NUMBER(1)
  /* -------------------------------------------------------------- */
  /* ALLOCATE SYSPLEX COUPLE DATASET XCFCDS03                       */
  /* -------------------------------------------------------------- */
  DEFINEDS SYSPLEX(LOCAL)
       DSN(Z24MST.SYS1.XCF.CDS03)
       VOLSER(ZOS247)
       MAXSYSTEM(8)
       CATALOG
    DATA TYPE(SYSPLEX)
       ITEM NAME(GROUP)    NUMBER(50)
       ITEM NAME(MEMBER)   NUMBER(100)
       ITEM NAME(GRS)      NUMBER(1)
/*
//NOTOK   EXEC PGM=CPPMAXRC,COND=((0,GE,ALCCDS),(4000,LT))
//*
//SETVARS  EXEC PGM=IKJEFT1B,COND=(4000,LT)
//*--------------------------------------------------------------------
//* DOC: STEP SETVARS UPDATES ORDER VARIABLE TABLE
//*--------------------------------------------------------------------
//SYSEXEC  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPCENU
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQEXEC
//         DD DISP=SHR,DSN=AOP.SAOPEXEC
//         DD DISP=SHR,DSN=EUVF.SEUVFEXC
//         DD DISP=SHR,DSN=SYS1.SEDGEXE1
//         DD DISP=SHR,DSN=EOY.SEOYCLIB
//         DD DISP=SHR,DSN=EOX.SEPHCLB1
//         DD DISP=SHR,DSN=ISP.SISPEXEC
//         DD DISP=SHR,DSN=IOE.SIOEEXEC
//         DD DISP=SHR,DSN=SYS1.SBPXEXEC
//         DD DISP=SHR,DSN=ISF.SISFEXEC
//         DD DISP=SHR,DSN=GLD.SGLDEXEC
//         DD DISP=SHR,DSN=FFST.V120ESA.SEPWSRC1
//SYSPROC  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPCENU
//         DD DISP=SHR,DSN=CPAC.CMDPROC
//         DD DISP=SHR,DSN=SYS1.SBLSCLI0
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJCLST
//         DD DISP=SHR,DSN=CBC.SCCNUTL
//         DD DISP=SHR,DSN=SYS1.DGTCLIB
//         DD DISP=SHR,DSN=CSF.SCSFCLI0
//         DD DISP=SHR,DSN=EOY.SEOYCLIB
//         DD DISP=SHR,DSN=EOX.SEPHCLB1
//         DD DISP=SHR,DSN=SYS1.SERBCLS
//         DD DISP=SHR,DSN=ISP.SISPCLIB
//         DD DISP=SHR,DSN=SYS1.HRFCLST
//         DD DISP=SHR,DSN=SYS1.SBDTCLI0
//         DD DISP=SHR,DSN=GIM.SGIMCLS0
//         DD DISP=SHR,DSN=ICQ.ICQCCLIB
//         DD DISP=SHR,DSN=SYS1.SCBDCLST
//         DD DISP=SHR,DSN=GDDM.SADMSAM
//         DD DISP=SHR,DSN=FFST.V120ESA.SEPWCENU
//ISPMLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPMENU
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJMLIB
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQMSGE
//         DD DISP=SHR,DSN=TCPIP.SEZAMENU
//         DD DISP=SHR,DSN=AOP.SAOPMENU
//         DD DISP=SHR,DSN=SYS1.DGTMLIB
//         DD DISP=SHR,DSN=SYS1.DFQMLIB
//         DD DISP=SHR,DSN=SYS1.SEDGMENU
//         DD DISP=SHR,DSN=CSF.SCSFMSG0
//         DD DISP=SHR,DSN=EOY.SEOYBENU
//         DD DISP=SHR,DSN=EOY.SEOYMENU
//         DD DISP=SHR,DSN=EOX.SEPHMSG1
//         DD DISP=SHR,DSN=SYS1.SERBMENU
//         DD DISP=SHR,DSN=ISP.SISPMENU
//         DD DISP=SHR,DSN=SYS1.HRFMSG
//         DD DISP=SHR,DSN=SYS1.SBDTMSG
//         DD DISP=SHR,DSN=SYS1.SBLSMSG0
//         DD DISP=SHR,DSN=SYS1.SBPXMENU
//         DD DISP=SHR,DSN=ISF.SISFMLIB
//         DD DISP=SHR,DSN=GIM.SGIMMENU
//         DD DISP=SHR,DSN=ICQ.ICQMLIB
//         DD DISP=SHR,DSN=SYS1.SCBDMENU
//         DD DISP=SHR,DSN=GDDM.SADMMSG
//ISPPLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPPENU
//         DD DISP=SHR,DSN=CPAC.ISPPLIB
//         DD DISP=SHR,DSN=ISP.SISPPENU
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJPLIB
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQPNLE
//         DD DISP=SHR,DSN=TCPIP.SEZAPENU
//         DD DISP=SHR,DSN=SYS1.SBLSPNL0
//         DD DISP=SHR,DSN=AOP.SAOPPENU
//         DD DISP=SHR,DSN=SYS1.DFQPLIB
//         DD DISP=SHR,DSN=SYS1.DGTPLIB
//         DD DISP=SHR,DSN=SYS1.SEDGPENU
//         DD DISP=SHR,DSN=CSF.SCSFPNL0
//         DD DISP=SHR,DSN=EOY.SEOYPENU
//         DD DISP=SHR,DSN=EOX.SEPHPNL1
//         DD DISP=SHR,DSN=SYS1.SERBPENU
//         DD DISP=SHR,DSN=SYS1.HRFPANL
//         DD DISP=SHR,DSN=SYS1.SBDTPNL0
//         DD DISP=SHR,DSN=SYS1.SHASPNL0
//         DD DISP=SHR,DSN=SYS1.SBPXPENU
//         DD DISP=SHR,DSN=ISF.SISFPLIB
//         DD DISP=SHR,DSN=GIM.SGIMPENU
//         DD DISP=SHR,DSN=ICQ.ICQPLIB
//         DD DISP=SHR,DSN=SYS1.SCBDPENU
//         DD DISP=SHR,DSN=GDDM.SADMPNL
//         DD DISP=SHR,DSN=FFST.V120ESA.SEPWPENU
//ISPSLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPSENU
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJSLIB
//         DD DISP=SHR,DSN=SYS1.DGTSLIB
//         DD DISP=SHR,DSN=CSF.SCSFSKL0
//         DD DISP=SHR,DSN=ISP.SISPSLIB
//         DD DISP=SHR,DSN=ISP.SISPSENU
//         DD DISP=SHR,DSN=SYS1.HRFSKEL
//         DD DISP=SHR,DSN=SYS1.SBLSKEL0
//         DD DISP=SHR,DSN=ISF.SISFSLIB
//         DD DISP=SHR,DSN=GIM.SGIMSENU
//         DD DISP=SHR,DSN=ICQ.ICQSLIB
//ISPLLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPLOAD
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJMOD1
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQANLE
//         DD DISP=SHR,DSN=SYS1.DFQLLIB
//         DD DISP=SHR,DSN=SYS1.DGTLLIB
//ISPTLIB  DD DISP=SHR,DSN=ZOS24.OS241147.ORDER.SCPPTENU
//         DD DISP=SHR,DSN=E002884.ISPF.ISPPROF
//         DD DISP=SHR,DSN=ABJ.H09F210.SABJTLIB
//         DD DISP=SHR,DSN=MQ.V8R0M0.SCSQTBLE
//         DD DISP=SHR,DSN=SYS1.DGTTLIB
//         DD DISP=SHR,DSN=CSF.SCSFTLIB
//         DD DISP=SHR,DSN=EOY.SEOYTENU
//         DD DISP=SHR,DSN=EOX.SEPHTBL1
//         DD DISP=SHR,DSN=SYS1.SERBT
//         DD DISP=SHR,DSN=SYS1.SERBTENU
//         DD DISP=SHR,DSN=ISP.SISPTENU
//         DD DISP=SHR,DSN=SYS1.SBLSTBL0
//         DD DISP=SHR,DSN=SYS1.SBPXTENU
//         DD DISP=SHR,DSN=ISF.SISFTLIB
//         DD DISP=SHR,DSN=GIM.SGIMTENU
//         DD DISP=SHR,DSN=ICQ.ICQTABLS
//         DD DISP=SHR,DSN=SYS1.SCBDTENU
//ISPPROF  DD UNIT=SYSALLDA,DISP=(NEW,DELETE),
//            DCB=(LRECL=80,BLKSIZE=3120,RECFM=FB),
//            SPACE=(CYL,(1,1,1))
//*
//ISPLOG   DD SYSOUT=*,DCB=(RECFM=VA,LRECL=125,BLKSIZE=129)
//PACINV   DD DSN=ZOS24.OS241147.SCPPVENU,
//            DISP=SHR,AMP='BUFSP=512000'
//MSGINV   DD DSN=ZOS24.OS241147.SCPPEENU,
//            DISP=SHR,AMP='BUFSP=512000'
//*
//SCPPTENU DD DSN=ZOS24.OS241147.ORDER.SCPPTENU,
//            DISP=SHR
//SYSTSPRT DD SYSOUT=*
//SYSPRINT DD SYSOUT=*
//SYSTSIN  DD *
  PROFILE  PREFIX(E002884)
  ISPSTART CMD(CPPEUPDV)
//VARSIN   DD *
FADAXX02 Y
/*
//NOTOK    EXEC PGM=CPPMAXRC,COND=((0,EQ,SETVARS),(4000,LT))
